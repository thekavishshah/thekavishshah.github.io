<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Kavish Shah — Portfolio</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://unpkg.com/aos@next/dist/aos.css" rel="stylesheet" />
  <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js"></script>
  <style>
    .typed-text::after {
      content: '|';
      animation: blink 0.7s infinite;
    }
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
    /* Scroll Progress Bar */
    #progressBar {
      height: 4px;
      background: linear-gradient(to right, #3b82f6, #0ea5e9);
      width: 0;
      position: fixed;
      top: 0;
      left: 0;
      z-index: 50;
    }

    /* Animated Gradient Background */
    #hero {
      background: linear-gradient(-45deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #4facfe 75%, #667eea 100%);
      background-size: 400% 400%;
      animation: gradientShift 15s ease infinite;
      position: relative;
      overflow: hidden;
    }

    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    /* Floating Particles */
    .particle {
      position: absolute;
      border-radius: 50%;
      pointer-events: none;
      opacity: 0.6;
      animation: float-particle 20s infinite;
    }

    @keyframes float-particle {
      0%, 100% { transform: translate(0, 0) rotate(0deg); opacity: 0.6; }
      25% { transform: translate(100px, -100px) rotate(90deg); opacity: 0.8; }
      50% { transform: translate(-50px, -200px) rotate(180deg); opacity: 0.4; }
      75% { transform: translate(-150px, -100px) rotate(270deg); opacity: 0.8; }
    }

    .particle:nth-child(1) { left: 10%; top: 20%; width: 80px; height: 80px; background: rgba(102, 126, 234, 0.4); animation-delay: 0s; }
    .particle:nth-child(2) { left: 80%; top: 30%; width: 60px; height: 60px; background: rgba(240, 147, 251, 0.4); animation-delay: 3s; }
    .particle:nth-child(3) { left: 60%; top: 70%; width: 100px; height: 100px; background: rgba(79, 172, 254, 0.3); animation-delay: 6s; }
    .particle:nth-child(4) { left: 20%; top: 80%; width: 70px; height: 70px; background: rgba(118, 75, 162, 0.4); animation-delay: 9s; }

    /* Gradient Text */
    .gradient-text {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 25%, #f093fb 50%, #00f2fe 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      animation: gradientTextShift 8s ease infinite;
      background-size: 300% 300%;
    }

    @keyframes gradientTextShift {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }

    /* Glassmorphism Container */
    .glass-container {
      position: relative;
      width: 600px;
      height: 600px;
      margin: 0 auto;
    }

    /* Glassmorphism Orbit Ring */
    .orbit-ring {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 500px;
      height: 500px;
      border-radius: 50%;
      border: 2px solid rgba(255, 255, 255, 0.2);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1), inset 0 0 40px rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      animation: pulse-ring 4s ease-in-out infinite;
    }

    @keyframes pulse-ring {
      0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.5; }
      50% { transform: translate(-50%, -50%) scale(1.05); opacity: 0.8; }
    }

    /* Glassmorphism Icons */
    .orbit-icon {
      position: absolute;
      width: 90px;
      height: 90px;
      border-radius: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1), 0 0 0 1px rgba(255, 255, 255, 0.1) inset;
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      cursor: pointer;
      z-index: 10;
    }

    .orbit-icon:hover {
      transform: scale(1.2) translateY(-5px);
      background: rgba(255, 255, 255, 0.25);
      box-shadow: 0 15px 45px rgba(0, 0, 0, 0.2), 0 0 60px rgba(102, 126, 234, 0.5);
      border: 1px solid rgba(255, 255, 255, 0.5);
    }

    .orbit-icon svg {
      width: 38px;
      height: 38px;
      margin-bottom: 6px;
      filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
    }

    .orbit-icon span {
      font-size: 11px;
      font-weight: 700;
      color: white;
      text-shadow: 0 2px 4px rgba(0,0,0,0.3);
      letter-spacing: 0.5px;
    }

    @keyframes orbit1 {
      from { transform: rotate(0deg) translateX(240px) rotate(0deg); }
      to { transform: rotate(360deg) translateX(240px) rotate(-360deg); }
    }

    @keyframes orbit2 {
      from { transform: rotate(60deg) translateX(240px) rotate(-60deg); }
      to { transform: rotate(420deg) translateX(240px) rotate(-420deg); }
    }

    @keyframes orbit3 {
      from { transform: rotate(120deg) translateX(240px) rotate(-120deg); }
      to { transform: rotate(480deg) translateX(240px) rotate(-480deg); }
    }

    @keyframes orbit4 {
      from { transform: rotate(180deg) translateX(240px) rotate(-180deg); }
      to { transform: rotate(540deg) translateX(240px) rotate(-540deg); }
    }

    @keyframes orbit5 {
      from { transform: rotate(240deg) translateX(240px) rotate(-240deg); }
      to { transform: rotate(600deg) translateX(240px) rotate(-600deg); }
    }

    @keyframes orbit6 {
      from { transform: rotate(300deg) translateX(240px) rotate(-300deg); }
      to { transform: rotate(660deg) translateX(240px) rotate(-660deg); }
    }

    .orbit-icon:nth-child(2) { animation: orbit1 25s linear infinite; }
    .orbit-icon:nth-child(3) { animation: orbit2 25s linear infinite; }
    .orbit-icon:nth-child(4) { animation: orbit3 25s linear infinite; }
    .orbit-icon:nth-child(5) { animation: orbit4 25s linear infinite; }
    .orbit-icon:nth-child(6) { animation: orbit5 25s linear infinite; }
    .orbit-icon:nth-child(7) { animation: orbit6 25s linear infinite; }

    /* Central 3D Workspace - Canvas Container */
    .coding-illustration {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 600px;
      height: 600px;
      z-index: 5;
    }

    #workspace3d {
      width: 100%;
      height: 100%;
      cursor: grab;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
    }

    #workspace3d:active {
      cursor: grabbing;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .glass-container {
        width: 380px;
        height: 380px;
      }

      .orbit-ring {
        width: 340px;
        height: 340px;
      }

      @keyframes orbit1 { from { transform: rotate(0deg) translateX(160px) rotate(0deg); } to { transform: rotate(360deg) translateX(160px) rotate(-360deg); } }
      @keyframes orbit2 { from { transform: rotate(60deg) translateX(160px) rotate(-60deg); } to { transform: rotate(420deg) translateX(160px) rotate(-420deg); } }
      @keyframes orbit3 { from { transform: rotate(120deg) translateX(160px) rotate(-120deg); } to { transform: rotate(480deg) translateX(160px) rotate(-480deg); } }
      @keyframes orbit4 { from { transform: rotate(180deg) translateX(160px) rotate(-180deg); } to { transform: rotate(540deg) translateX(160px) rotate(-540deg); } }
      @keyframes orbit5 { from { transform: rotate(240deg) translateX(160px) rotate(-240deg); } to { transform: rotate(600deg) translateX(160px) rotate(-600deg); } }
      @keyframes orbit6 { from { transform: rotate(300deg) translateX(160px) rotate(-300deg); } to { transform: rotate(660deg) translateX(160px) rotate(-660deg); } }

      .orbit-icon {
        width: 70px;
        height: 70px;
        border-radius: 16px;
      }

      .orbit-icon svg {
        width: 28px;
        height: 28px;
      }

      .orbit-icon span {
        font-size: 9px;
      }

      .coding-illustration {
        width: 400px;
        height: 400px;
      }

      .particle {
        display: none;
      }
    }
  </style>
</head>
<body class="bg-gradient-to-b from-gray-50 to-white text-gray-800 font-sans relative">
  <div id="progressBar"></div>

  <!-- Navigation -->
  <header class="fixed w-full bg-white/70 backdrop-blur-md shadow-md z-20 transition-colors duration-300" data-aos="fade-down">
    <nav class="max-w-5xl mx-auto flex justify-between items-center p-4">
      <a href="#hero" class="text-2xl font-bold hover:text-blue-600 transition">Kavish Shah</a>
      <div class="space-x-6 flex items-center">
        <a href="KAVISH_SHAH_RESUME-MAY.pdf" target="_blank" class="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700 transition">Resume</a>
        <a href="#about" class="hover:text-blue-600 transition">About</a>
        <a href="#skills" class="hover:text-blue-600 transition">Skills</a>
        <a href="#work" class="hover:text-blue-600 transition">Work</a>
        <a href="#campus" class="hover:text-blue-600 transition">Campus</a>
        <a href="#projects" class="hover:text-blue-600 transition">Projects</a>
        <a href="#contact" class="hover:text-blue-600 transition">Contact</a>
      </div>
    </nav>
  </header>

  </header>

  <!-- Hero -->
  <section id="hero" class="min-h-screen flex flex-col justify-center items-center text-center p-6 pt-24" data-aos="fade-up">
    <h1 class="text-4xl md:text-6xl font-extrabold mb-2 gradient-text drop-shadow-lg">Hi, I'm Kavish Shah</h1>
    <p class="text-lg md:text-xl mb-12 text-white drop-shadow-md">
      <span class="typed-text font-semibold"></span>
    </p>

    <!-- Interactive Glass Container -->
    <div class="glass-container">
      <!-- Central Coding Illustration -->
      <div class="coding-illustration">
        <canvas id="workspace3d"></canvas>
      </div>
    </div>

    <p class="mt-12 text-sm text-white drop-shadow-md max-w-2xl font-medium text-center">
      Drag to rotate • Scroll to zoom • Watch the AI developer at work
    </p>
  </section>

  <!-- About -->
  <section id="about" class="py-20 px-6 max-w-3xl mx-auto" data-aos="fade-right">
    <h2 class="text-3xl font-semibold text-center mb-6">About Me</h2>
    <div class="flex justify-center mb-6">
      <img src="images/profile.jpg" alt="Kavish Shah" class="w-72 h-72 md:w-96 md:h-96 rounded-lg shadow-xl border-4 border-blue-500 object-cover hover:scale-105 transform transition" />
    </div>
    <p class="text-gray-700 leading-relaxed">
      I’m a junior at Arizona State University pursuing a BS (Honors) in Computer Science. I mentor first-year engineers, served as Treasurer of SoDA, and love building web apps, exploring cybersecurity, and leveraging AI for real-world solutions.
    </p>
  </section>

  <!-- Technical Skills -->
  <section id="skills" class="py-20 px-6 max-w-3xl mx-auto bg-gradient-to-r from-blue-50 to-indigo-50 rounded-2xl" data-aos="zoom-in">
    <h2 class="text-3xl font-semibold text-center mb-6">Technical Skills</h2>
    <div class="flex flex-wrap justify-center gap-4 mt-4">
      <!-- individual badges animated -->
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left">JavaScript</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="100">Java</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="200">JavaFX</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="300">Python</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="400">C</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="500">C++</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="600">CSS</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="700">HTML</span>
      <span class="px-4 py-2 bg-blue-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="800">MATLAB</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="900">ReactJS</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1000">Next.js</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1100">Prisma</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1200">NodeJS</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1300">Express</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1400">Flask</span>
      <span class="px-4 py-2 bg-green-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1500">Django REST Framework</span>
      <span class="px-4 py-2 bg-yellow-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1600">PostgreSQL</span>
      <span class="px-4 py-2 bg-yellow-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1700">MySQL</span>
      <span class="px-4 py-2 bg-yellow-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1800">MongoDB</span>
      <span class="px-4 py-2 bg-indigo-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="1900">Git</span>
      <span class="px-4 py-2 bg-indigo-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2000">JIRA</span>
      <span class="px-4 py-2 bg-indigo-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2100">Docker</span>
      <span class="px-4 py-2 bg-indigo-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2200">CI/CD Pipeline</span>
      <span class="px-4 py-2 bg-indigo-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2300">Agile/Scrum</span>
      <span class="px-4 py-2 bg-indigo-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2400">Salesforce</span>
      <span class="px-4 py-2 bg-pink-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2500">Ubuntu</span>
      <span class="px-4 py-2 bg-pink-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2600">Verilog</span>
      <span class="px-4 py-2 bg-pink-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2700">Scheme</span>
      <span class="px-4 py-2 bg-pink-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2800">Prolog</span>
      <span class="px-4 py-2 bg-pink-100 rounded-full hover:shadow-lg transform hover:-translate-y-1 transition" data-aos="flip-left" data-aos-delay="2900">Microsoft Office Suite</span>
    </div>
  </section>

  <!-- Other sections retain AOS attributes and enhanced hover effects, etc. -->

  <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
  <script>
    AOS.init({ duration: 800, once: true });

    // ==========================================
    // THREE.JS 3D WORKSPACE SCENE
    // ==========================================

    let scene, camera, renderer, controls;
    let workspace3dGroup;

    function init3DWorkspace() {
      const canvas = document.getElementById('workspace3d');
      const container = canvas.parentElement;

      // Scene Setup
      scene = new THREE.Scene();
      // Transparent background to blend with hero gradient
      scene.background = null;

      // Camera - right side view for diorama effect
      camera = new THREE.PerspectiveCamera(
        40,
        container.clientWidth / container.clientHeight,
        0.1,
        1000
      );
      camera.position.set(15, 10, 5);
      camera.lookAt(0, 2.5, 0);

      // Renderer
      renderer = new THREE.WebGLRenderer({
        canvas: canvas,
        antialias: true,
        alpha: true
      });
      renderer.setSize(container.clientWidth, container.clientHeight);
      renderer.setPixelRatio(window.devicePixelRatio);
      renderer.shadowMap.enabled = true;
      renderer.shadowMap.type = THREE.PCFSoftShadowMap;

      // OrbitControls for 360-degree rotation
      controls = new THREE.OrbitControls(camera, renderer.domElement);
      controls.enableDamping = true;
      controls.dampingFactor = 0.08;
      controls.minDistance = 8;
      controls.maxDistance = 35;
      controls.maxPolarAngle = Math.PI / 2.1;
      controls.minPolarAngle = Math.PI / 6;
      controls.autoRotate = true;
      controls.autoRotateSpeed = 0.8;
      controls.enablePan = false;
      controls.target.set(0, 2, 0);

      // Soft pastel lighting for diorama aesthetic
      const ambientLight = new THREE.AmbientLight(0xffffff, 0.8);
      scene.add(ambientLight);

      // Main soft directional light
      const mainLight = new THREE.DirectionalLight(0xffffff, 0.6);
      mainLight.position.set(15, 20, 15);
      mainLight.castShadow = true;
      mainLight.shadow.mapSize.width = 2048;
      mainLight.shadow.mapSize.height = 2048;
      mainLight.shadow.camera.left = -15;
      mainLight.shadow.camera.right = 15;
      mainLight.shadow.camera.top = 15;
      mainLight.shadow.camera.bottom = -15;
      scene.add(mainLight);

      // Soft fill light from side
      const fillLight = new THREE.DirectionalLight(0xb8d4f1, 0.4);
      fillLight.position.set(-10, 10, 5);
      scene.add(fillLight);

      // Gentle rim light
      const rimLight = new THREE.DirectionalLight(0xa8c8e6, 0.3);
      rimLight.position.set(5, 5, -10);
      scene.add(rimLight);

      // Group for entire workspace
      workspace3dGroup = new THREE.Group();
      scene.add(workspace3dGroup);

      // Build Isometric Diorama Scene
      createPlatformBase();
      createRoomBox();
      createMiniDesk();
      createMiniChair();
      createMiniMonitor();
      createMiniKeyboard();
      createCPUTower();
      createSideTable();
      createShelves();
      createWallDecorations();
      createFloorItems();
      createPlants();

      // Handle resize
      window.addEventListener('resize', onWindowResize, false);

      // Animation loop
      animate();
    }

    // ===== DIORAMA CREATION FUNCTIONS =====

    function createPlatformBase() {
      // Circular platform base - soft pastel blue
      const platformGeometry = new THREE.CylinderGeometry(5.5, 5.5, 0.3, 64);
      const platformMaterial = new THREE.MeshStandardMaterial({
        color: 0xd4e8f7,
        roughness: 0.7
      });
      const platform = new THREE.Mesh(platformGeometry, platformMaterial);
      platform.position.y = 0;
      platform.receiveShadow = true;
      workspace3dGroup.add(platform);

      // Platform rim (decorative edge) - light blue
      const rimGeometry = new THREE.TorusGeometry(5.5, 0.08, 16, 64);
      const rimMaterial = new THREE.MeshStandardMaterial({
        color: 0xc4d8e7,
        roughness: 0.5
      });
      const rim = new THREE.Mesh(rimGeometry, rimMaterial);
      rim.rotation.x = Math.PI / 2;
      rim.position.y = 0.15;
      workspace3dGroup.add(rim);
    }

    function createRoomBox() {
      // Back wall - very light pastel lavender/white
      const backWallGeometry = new THREE.BoxGeometry(6, 5, 0.2);
      const wallMaterial = new THREE.MeshStandardMaterial({
        color: 0xf5f5ff,
        roughness: 0.85
      });
      const backWall = new THREE.Mesh(backWallGeometry, wallMaterial);
      backWall.position.set(0, 2.8, -3);
      backWall.receiveShadow = true;
      workspace3dGroup.add(backWall);

      // Left wall only (removed right wall for open view)
      const leftWall = new THREE.Mesh(new THREE.BoxGeometry(0.2, 5, 6), wallMaterial);
      leftWall.position.set(-3, 2.8, 0);
      leftWall.receiveShadow = true;
      workspace3dGroup.add(leftWall);

      // Roof overhang (light warm wood)
      const roofGeometry = new THREE.BoxGeometry(6.5, 0.3, 6.5);
      const roofMaterial = new THREE.MeshStandardMaterial({
        color: 0xe8c8a4,
        roughness: 0.8
      });
      const roof = new THREE.Mesh(roofGeometry, roofMaterial);
      roof.position.set(0, 5.3, 0);
      roof.castShadow = true;
      workspace3dGroup.add(roof);

      // Floor (light wooden planks)
      const floorGeometry = new THREE.BoxGeometry(5.8, 0.15, 5.8);
      const floorMaterial = new THREE.MeshStandardMaterial({
        color: 0xf0dcc0,
        roughness: 0.85
      });
      const floor = new THREE.Mesh(floorGeometry, floorMaterial);
      floor.position.set(0, 0.3, 0);
      floor.receiveShadow = true;
      workspace3dGroup.add(floor);

      // Add floor planks detail - lighter
      for (let i = 0; i < 10; i++) {
        const plankLine = new THREE.Mesh(
          new THREE.BoxGeometry(5.8, 0.02, 0.05),
          new THREE.MeshStandardMaterial({ color: 0xe8d4b8 })
        );
        plankLine.position.set(0, 0.38, -2.5 + i * 0.6);
        workspace3dGroup.add(plankLine);
      }
    }

    function createMiniDesk() {
      // Larger desk surface (light wood) - increased size for more workspace
      const deskGeometry = new THREE.BoxGeometry(4.5, 0.18, 2.5);
      const deskMaterial = new THREE.MeshStandardMaterial({
        color: 0xf5e6d3,
        roughness: 0.6
      });
      const desk = new THREE.Mesh(deskGeometry, deskMaterial);
      desk.position.set(0, 1.2, 0.3);
      desk.castShadow = true;
      desk.receiveShadow = true;
      workspace3dGroup.add(desk);

      // Desk drawer unit (pastel lavender)
      const drawerGeometry = new THREE.BoxGeometry(0.9, 0.95, 0.7);
      const drawerMaterial = new THREE.MeshStandardMaterial({
        color: 0xe8dff5,
        roughness: 0.6
      });
      const drawer = new THREE.Mesh(drawerGeometry, drawerMaterial);
      drawer.position.set(-1.5, 0.65, 0.3);
      drawer.castShadow = true;
      workspace3dGroup.add(drawer);

      // Drawer handles (3 drawers) - soft gold/brass
      for (let i = 0; i < 3; i++) {
        const handle = new THREE.Mesh(
          new THREE.BoxGeometry(0.18, 0.05, 0.05),
          new THREE.MeshStandardMaterial({
            color: 0xd4b896,
            roughness: 0.4,
            metalness: 0.5
          })
        );
        handle.position.set(-1.5, 0.35 + i * 0.32, 0.65);
        workspace3dGroup.add(handle);
      }

      // Desk legs (simple blue/white) - adjusted for larger desk
      const legPositions = [[1.8, -0.6], [-1.8, -0.6], [1.8, 1.2], [-1.8, 1.2]];
      legPositions.forEach(([x, z]) => {
        const leg = new THREE.Mesh(
          new THREE.CylinderGeometry(0.08, 0.08, 1.2, 16),
          new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.4 })
        );
        leg.position.set(x, 0.6, z);
        leg.castShadow = true;
        workspace3dGroup.add(leg);
      });

      // Desk organizers and items
      // Small organizer box
      const organizer = new THREE.Mesh(
        new THREE.BoxGeometry(0.4, 0.15, 0.3),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.7 })
      );
      organizer.position.set(-0.8, 1.37, -0.8);
      workspace3dGroup.add(organizer);

      // Pen holder
      const penHolder = new THREE.Mesh(
        new THREE.CylinderGeometry(0.12, 0.12, 0.25, 16),
        new THREE.MeshStandardMaterial({ color: 0xd4e8f7, roughness: 0.6 })
      );
      penHolder.position.set(1.2, 1.42, -0.7);
      workspace3dGroup.add(penHolder);

      // Pens in holder
      for (let i = 0; i < 3; i++) {
        const pen = new THREE.Mesh(
          new THREE.CylinderGeometry(0.02, 0.02, 0.35, 8),
          new THREE.MeshStandardMaterial({ color: [0x4a8bb7, 0x7eb3d9, 0x5a9bc7][i] })
        );
        pen.position.set(1.2 + (i - 1) * 0.04, 1.55, -0.7);
        workspace3dGroup.add(pen);
      }

      // Small books stack on desk
      const bookColors = [0x7eb3d9, 0x5a9bc7, 0xb8d4f1];
      bookColors.forEach((color, i) => {
        const book = new THREE.Mesh(
          new THREE.BoxGeometry(0.3, 0.04, 0.22),
          new THREE.MeshStandardMaterial({ color: color, roughness: 0.8 })
        );
        book.position.set(0.8, 1.32 + i * 0.045, -0.8);
        book.rotation.y = 0.1;
        workspace3dGroup.add(book);
      });

      // Coffee mug
      const mug = new THREE.Mesh(
        new THREE.CylinderGeometry(0.1, 0.08, 0.15, 16),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.5 })
      );
      mug.position.set(1.3, 1.37, 0.8);
      workspace3dGroup.add(mug);

      // Notepad
      const notepad = new THREE.Mesh(
        new THREE.BoxGeometry(0.35, 0.02, 0.45),
        new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.9 })
      );
      notepad.position.set(0.2, 1.31, 0.6);
      notepad.rotation.y = -0.3;
      workspace3dGroup.add(notepad);

      // Desk lamp
      const lampBase = new THREE.Mesh(
        new THREE.CylinderGeometry(0.15, 0.15, 0.05, 32),
        new THREE.MeshStandardMaterial({ color: 0x4a8bb7, roughness: 0.5 })
      );
      lampBase.position.set(-1.5, 1.32, -0.9);
      workspace3dGroup.add(lampBase);

      const lampArm = new THREE.Mesh(
        new THREE.CylinderGeometry(0.03, 0.03, 0.6, 16),
        new THREE.MeshStandardMaterial({ color: 0x5a9bc7, roughness: 0.4 })
      );
      lampArm.position.set(-1.5, 1.65, -0.9);
      lampArm.rotation.z = 0.3;
      workspace3dGroup.add(lampArm);

      const lampHead = new THREE.Mesh(
        new THREE.CylinderGeometry(0.12, 0.18, 0.15, 32),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.5 })
      );
      lampHead.position.set(-1.35, 1.9, -0.9);
      workspace3dGroup.add(lampHead);

      // Lamp light
      const lampLight = new THREE.PointLight(0xffe6a8, 0.6, 3);
      lampLight.position.set(-1.35, 1.85, -0.9);
      scene.add(lampLight);

      // Water bottle
      const bottle = new THREE.Mesh(
        new THREE.CylinderGeometry(0.08, 0.08, 0.35, 16),
        new THREE.MeshStandardMaterial({
          color: 0x7eb3d9,
          transparent: true,
          opacity: 0.7,
          roughness: 0.2
        })
      );
      bottle.position.set(-1.0, 1.47, 0.9);
      workspace3dGroup.add(bottle);

      const bottleCap = new THREE.Mesh(
        new THREE.CylinderGeometry(0.09, 0.09, 0.06, 16),
        new THREE.MeshStandardMaterial({ color: 0x4a8bb7 })
      );
      bottleCap.position.set(-1.0, 1.67, 0.9);
      workspace3dGroup.add(bottleCap);

      // Phone
      const phone = new THREE.Mesh(
        new THREE.BoxGeometry(0.15, 0.02, 0.28),
        new THREE.MeshStandardMaterial({
          color: 0x2a3a4a,
          roughness: 0.2,
          metalness: 0.7
        })
      );
      phone.position.set(-0.5, 1.31, 0.9);
      phone.rotation.y = 0.4;
      workspace3dGroup.add(phone);

      // Headphones
      const headphoneStand = new THREE.Mesh(
        new THREE.CylinderGeometry(0.08, 0.12, 0.45, 16),
        new THREE.MeshStandardMaterial({ color: 0x2a3a4a, metalness: 0.6 })
      );
      headphoneStand.position.set(1.6, 1.52, -0.9);
      workspace3dGroup.add(headphoneStand);

      const headphoneBand = new THREE.Mesh(
        new THREE.TorusGeometry(0.15, 0.03, 16, 32, Math.PI),
        new THREE.MeshStandardMaterial({ color: 0x4a8bb7, roughness: 0.4 })
      );
      headphoneBand.position.set(1.6, 1.75, -0.9);
      headphoneBand.rotation.x = Math.PI / 2;
      workspace3dGroup.add(headphoneBand);

      // Extra notebooks stack
      const notebookColors = [0xff8585, 0xffd485, 0x85e0ff];
      notebookColors.forEach((color, i) => {
        const notebook = new THREE.Mesh(
          new THREE.BoxGeometry(0.25, 0.03, 0.32),
          new THREE.MeshStandardMaterial({ color: color, roughness: 0.8 })
        );
        notebook.position.set(-0.2, 1.31 + i * 0.035, -0.9);
        notebook.rotation.y = -0.2;
        workspace3dGroup.add(notebook);
      });

      // Charging cable (simplified)
      const cable = new THREE.Mesh(
        new THREE.CylinderGeometry(0.015, 0.015, 0.4, 8),
        new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.5 })
      );
      cable.position.set(0.5, 1.31, 0.95);
      cable.rotation.z = Math.PI / 2;
      workspace3dGroup.add(cable);

      // Mouse pad
      const mousePad = new THREE.Mesh(
        new THREE.BoxGeometry(0.9, 0.01, 0.7),
        new THREE.MeshStandardMaterial({ color: 0x6a8db8, roughness: 0.9 })
      );
      mousePad.position.set(0.7, 1.295, 0.85);
      workspace3dGroup.add(mousePad);

      // Sticky notes stack (pastel colors)
      const stickyColors = [0xffd4e5, 0xd4f1f4, 0xfff4c4, 0xe4d4f4];
      stickyColors.forEach((color, i) => {
        const sticky = new THREE.Mesh(
          new THREE.BoxGeometry(0.18, 0.008, 0.18),
          new THREE.MeshStandardMaterial({ color: color, roughness: 0.9 })
        );
        sticky.position.set(-1.8, 1.3 + i * 0.01, 0.5);
        sticky.rotation.y = Math.random() * 0.4 - 0.2;
        workspace3dGroup.add(sticky);
      });

      // Pencil on desk
      const pencil = new THREE.Mesh(
        new THREE.CylinderGeometry(0.015, 0.015, 0.4, 8),
        new THREE.MeshStandardMaterial({ color: 0xf4d03f, roughness: 0.7 })
      );
      pencil.position.set(0.4, 1.3, -0.6);
      pencil.rotation.z = Math.PI / 2;
      workspace3dGroup.add(pencil);

      // Eraser
      const eraser = new THREE.Mesh(
        new THREE.BoxGeometry(0.08, 0.04, 0.15),
        new THREE.MeshStandardMaterial({ color: 0xffb3ba, roughness: 0.8 })
      );
      eraser.position.set(0.6, 1.31, -0.6);
      workspace3dGroup.add(eraser);

      // Wireless earbuds case
      const earbudsCase = new THREE.Mesh(
        new THREE.BoxGeometry(0.12, 0.05, 0.15),
        new THREE.MeshStandardMaterial({
          color: 0xf5f5f5,
          roughness: 0.3,
          metalness: 0.2
        })
      );
      earbudsCase.position.set(-1.3, 1.315, 0.5);
      earbudsCase.rotation.y = 0.5;
      workspace3dGroup.add(earbudsCase);

      // Small succulent plant on desk
      const smallPot = new THREE.Mesh(
        new THREE.CylinderGeometry(0.08, 0.06, 0.12, 16),
        new THREE.MeshStandardMaterial({ color: 0xe8d4c8, roughness: 0.8 })
      );
      smallPot.position.set(-1.8, 1.35, -0.8);
      workspace3dGroup.add(smallPot);

      // Succulent leaves
      for (let i = 0; i < 5; i++) {
        const angle = (i / 5) * Math.PI * 2;
        const leaf = new THREE.Mesh(
          new THREE.SphereGeometry(0.05, 8, 8),
          new THREE.MeshStandardMaterial({ color: 0x7eb881, roughness: 0.7 })
        );
        leaf.position.set(
          -1.8 + Math.cos(angle) * 0.06,
          1.42,
          -0.8 + Math.sin(angle) * 0.06
        );
        leaf.scale.set(1, 0.6, 1);
        workspace3dGroup.add(leaf);
      }

      // Coaster under mug
      const coaster = new THREE.Mesh(
        new THREE.CylinderGeometry(0.12, 0.12, 0.01, 32),
        new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.9 })
      );
      coaster.position.set(1.3, 1.30, 0.8);
      workspace3dGroup.add(coaster);

      // USB drive
      const usbDrive = new THREE.Mesh(
        new THREE.BoxGeometry(0.06, 0.015, 0.12),
        new THREE.MeshStandardMaterial({
          color: 0x4a8bb7,
          roughness: 0.4,
          metalness: 0.6
        })
      );
      usbDrive.position.set(-0.9, 1.3, 0.4);
      workspace3dGroup.add(usbDrive);

      // Paper clips scattered
      for (let i = 0; i < 3; i++) {
        const clip = new THREE.Mesh(
          new THREE.TorusGeometry(0.03, 0.008, 8, 16),
          new THREE.MeshStandardMaterial({
            color: 0xc0c0c0,
            metalness: 0.8,
            roughness: 0.2
          })
        );
        clip.position.set(-0.5 + i * 0.15, 1.3, -0.7);
        clip.rotation.x = Math.PI / 2;
        workspace3dGroup.add(clip);
      }

      // Highlighter
      const highlighter = new THREE.Mesh(
        new THREE.CylinderGeometry(0.02, 0.02, 0.3, 8),
        new THREE.MeshStandardMaterial({ color: 0xffeb3b, roughness: 0.5 })
      );
      highlighter.position.set(0.15, 1.3, -0.5);
      highlighter.rotation.z = Math.PI / 2.5;
      workspace3dGroup.add(highlighter);

      // Tape dispenser
      const tapeBase = new THREE.Mesh(
        new THREE.BoxGeometry(0.18, 0.08, 0.22),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.6 })
      );
      tapeBase.position.set(1.5, 1.33, -0.5);
      workspace3dGroup.add(tapeBase);

      const tapeRoll = new THREE.Mesh(
        new THREE.TorusGeometry(0.08, 0.03, 16, 32),
        new THREE.MeshStandardMaterial({
          color: 0xf5f5f5,
          transparent: true,
          opacity: 0.8,
          roughness: 0.2
        })
      );
      tapeRoll.position.set(1.5, 1.37, -0.5);
      tapeRoll.rotation.x = Math.PI / 2;
      workspace3dGroup.add(tapeRoll);
    }

    function createMiniChair() {
      // Chair seat (pastel purple/lavender to match reference)
      const seatGeometry = new THREE.CylinderGeometry(0.5, 0.45, 0.12, 32);
      const seatMaterial = new THREE.MeshStandardMaterial({
        color: 0xd4c4e8,
        roughness: 0.6
      });
      const seat = new THREE.Mesh(seatGeometry, seatMaterial);
      seat.position.set(0.1, 0.9, 2.2);
      seat.castShadow = true;
      workspace3dGroup.add(seat);

      // Chair back (curved) - positioned BEHIND the seat so chair faces desk
      const backGeometry = new THREE.BoxGeometry(0.9, 0.65, 0.1);
      const back = new THREE.Mesh(backGeometry, seatMaterial);
      back.position.set(0.1, 1.35, 2.45);
      back.rotation.x = 0.15;
      back.castShadow = true;
      workspace3dGroup.add(back);

      // Chair base (white)
      const baseGeometry = new THREE.CylinderGeometry(0.06, 0.06, 0.75, 16);
      const baseMaterial = new THREE.MeshStandardMaterial({
        color: 0xf5f5f5,
        roughness: 0.3,
        metalness: 0.3
      });
      const base = new THREE.Mesh(baseGeometry, baseMaterial);
      base.position.set(0.1, 0.5, 2.2);
      workspace3dGroup.add(base);

      // Chair wheels (5 spokes)
      for (let i = 0; i < 5; i++) {
        const angle = (i / 5) * Math.PI * 2;
        const wheel = new THREE.Mesh(
          new THREE.SphereGeometry(0.08, 16, 16),
          baseMaterial
        );
        wheel.position.set(
          0.1 + Math.cos(angle) * 0.38,
          0.15,
          2.2 + Math.sin(angle) * 0.38
        );
        workspace3dGroup.add(wheel);
      }
    }

    function createMiniMonitor() {
      // Create canvas textures for monitors
      const vscodeCanvas = createVSCodeCanvas();
      const terminalCanvas = createTerminalCanvas();

      const vscodeTexture = new THREE.CanvasTexture(vscodeCanvas);
      const terminalTexture = new THREE.CanvasTexture(terminalCanvas);

      // LEFT MONITOR - VSCode
      // Monitor stand
      const standMaterial = new THREE.MeshStandardMaterial({
        color: 0xffffff,
        roughness: 0.3,
        metalness: 0.6
      });

      const leftStand = new THREE.Mesh(
        new THREE.CylinderGeometry(0.1, 0.12, 0.5, 32),
        standMaterial
      );
      leftStand.position.set(-1.0, 1.6, -0.3);
      workspace3dGroup.add(leftStand);

      // Monitor frame (white/light blue)
      const frameMaterial = new THREE.MeshStandardMaterial({
        color: 0xf0f8ff,
        roughness: 0.4
      });

      const leftFrame = new THREE.Mesh(
        new THREE.BoxGeometry(1.2, 0.85, 0.08),
        frameMaterial
      );
      leftFrame.position.set(-1.0, 2.15, -0.3);
      leftFrame.castShadow = true;
      workspace3dGroup.add(leftFrame);

      // Screen with VSCode texture
      const leftScreenMaterial = new THREE.MeshStandardMaterial({
        map: vscodeTexture,
        emissive: 0x1a1a1a,
        emissiveIntensity: 0.3,
        roughness: 0.2
      });

      const leftScreen = new THREE.Mesh(
        new THREE.BoxGeometry(1.1, 0.75, 0.03),
        leftScreenMaterial
      );
      leftScreen.position.set(-1.0, 2.15, -0.26);
      workspace3dGroup.add(leftScreen);

      // Screen glow
      const leftGlow = new THREE.PointLight(0x4a9eff, 0.4, 2);
      leftGlow.position.set(-1.0, 2.15, -0.2);
      scene.add(leftGlow);

      // RIGHT MONITOR - Terminal
      const rightStand = new THREE.Mesh(
        new THREE.CylinderGeometry(0.1, 0.12, 0.5, 32),
        standMaterial
      );
      rightStand.position.set(1.0, 1.6, -0.3);
      workspace3dGroup.add(rightStand);

      const rightFrame = new THREE.Mesh(
        new THREE.BoxGeometry(1.2, 0.85, 0.08),
        frameMaterial
      );
      rightFrame.position.set(1.0, 2.15, -0.3);
      rightFrame.castShadow = true;
      workspace3dGroup.add(rightFrame);

      // Screen with Terminal texture
      const rightScreenMaterial = new THREE.MeshStandardMaterial({
        map: terminalTexture,
        emissive: 0x0a0a0a,
        emissiveIntensity: 0.3,
        roughness: 0.2
      });

      const rightScreen = new THREE.Mesh(
        new THREE.BoxGeometry(1.1, 0.75, 0.03),
        rightScreenMaterial
      );
      rightScreen.position.set(1.0, 2.15, -0.26);
      workspace3dGroup.add(rightScreen);

      const rightGlow = new THREE.PointLight(0x33ff33, 0.3, 2);
      rightGlow.position.set(1.0, 2.15, -0.2);
      scene.add(rightGlow);
    }

    function createVSCodeCanvas() {
      const canvas = document.createElement('canvas');
      canvas.width = 512;
      canvas.height = 384;
      const ctx = canvas.getContext('2d');

      // Background - dark theme
      ctx.fillStyle = '#1e1e1e';
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      // Top bar
      ctx.fillStyle = '#323233';
      ctx.fillRect(0, 0, canvas.width, 35);

      // VSCode logo area
      ctx.fillStyle = '#007acc';
      ctx.fillRect(5, 8, 20, 20);

      // Menu items
      ctx.fillStyle = '#cccccc';
      ctx.font = '12px monospace';
      ctx.fillText('File  Edit  View  Terminal', 35, 23);

      // Sidebar (file explorer)
      ctx.fillStyle = '#252526';
      ctx.fillRect(0, 35, 120, canvas.height - 35);

      // Sidebar header
      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 11px sans-serif';
      ctx.fillText('EXPLORER', 10, 55);

      // Folder icons and files
      const files = ['▼ src', '  index.js', '  app.js', '  utils.js', '▼ components', '  Header.jsx', '  Footer.jsx'];
      ctx.font = '10px monospace';
      files.forEach((file, i) => {
        ctx.fillStyle = file.startsWith('▼') ? '#ffffff' : '#cccccc';
        ctx.fillText(file, 15, 75 + i * 18);
      });

      // Editor area
      ctx.fillStyle = '#1e1e1e';
      ctx.fillRect(120, 35, canvas.width - 120, canvas.height - 115);

      // Tab
      ctx.fillStyle = '#2d2d30';
      ctx.fillRect(120, 35, 120, 35);
      ctx.fillStyle = '#ffffff';
      ctx.font = '11px sans-serif';
      ctx.fillText('app.js', 130, 57);

      // Code content
      const code = [
        'import React from "react";',
        '',
        'function App() {',
        '  const [data, setData] = useState();',
        '',
        '  useEffect(() => {',
        '    fetchData();',
        '  }, []);',
        '',
        '  return (',
        '    <div className="App">',
        '      <h1>Hello World</h1>',
        '    </div>',
        '  );',
        '}'
      ];

      ctx.font = '10px monospace';
      code.forEach((line, i) => {
        let color = '#cccccc';
        if (line.includes('import') || line.includes('function') || line.includes('return')) color = '#c586c0';
        if (line.includes('React') || line.includes('useState') || line.includes('useEffect')) color = '#4ec9b0';
        if (line.includes('"')) color = '#ce9178';
        if (line.includes('const') || line.includes('let')) color = '#569cd6';

        ctx.fillStyle = color;
        ctx.fillText(line, 130, 85 + i * 14);
      });

      // Terminal section at bottom
      ctx.fillStyle = '#1e1e1e';
      ctx.fillRect(120, canvas.height - 80, canvas.width - 120, 80);

      // Terminal tab
      ctx.fillStyle = '#252526';
      ctx.fillRect(120, canvas.height - 80, canvas.width - 120, 25);
      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 10px sans-serif';
      ctx.fillText('TERMINAL', 130, canvas.height - 65);

      // Terminal content
      ctx.fillStyle = '#cccccc';
      ctx.font = '9px monospace';
      ctx.fillText('$ npm run dev', 130, canvas.height - 45);
      ctx.fillStyle = '#4ec9b0';
      ctx.fillText('> Starting development server...', 130, canvas.height - 30);
      ctx.fillText('> Server running on port 3000', 130, canvas.height - 15);

      // Copilot panel on right (simplified)
      ctx.fillStyle = '#252526';
      ctx.fillRect(canvas.width - 90, 70, 90, 150);
      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 9px sans-serif';
      ctx.fillText('COPILOT', canvas.width - 85, 85);
      ctx.font = '8px sans-serif';
      ctx.fillStyle = '#cccccc';
      ctx.fillText('✓ Suggestions', canvas.width - 85, 105);
      ctx.fillText('  available', canvas.width - 85, 118);

      return canvas;
    }

    function createTerminalCanvas() {
      const canvas = document.createElement('canvas');
      canvas.width = 512;
      canvas.height = 384;
      const ctx = canvas.getContext('2d');

      // Terminal background - dark
      ctx.fillStyle = '#0c0c0c';
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      // Terminal header bar
      ctx.fillStyle = '#1a1a1a';
      ctx.fillRect(0, 0, canvas.width, 30);
      ctx.fillStyle = '#33ff33';
      ctx.font = 'bold 12px monospace';
      ctx.fillText('bash', 10, 20);

      // Terminal content
      const terminalLines = [
        { text: '$ python train_model.py --epochs 100', color: '#33ff33' },
        { text: '', color: '#ffffff' },
        { text: 'Epoch 1/100', color: '#ffffff' },
        { text: '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100%', color: '#5555ff' },
        { text: 'loss: 0.4521 - accuracy: 0.8234', color: '#ffff55' },
        { text: '', color: '#ffffff' },
        { text: 'Epoch 2/100', color: '#ffffff' },
        { text: '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 100%', color: '#5555ff' },
        { text: 'loss: 0.3891 - accuracy: 0.8567', color: '#ffff55' },
        { text: '', color: '#ffffff' },
        { text: 'Epoch 3/100', color: '#ffffff' },
        { text: '━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 82%', color: '#5555ff' },
        { text: 'loss: 0.3421 - accuracy: 0.8734', color: '#ffff55' },
        { text: '', color: '#ffffff' },
        { text: 'Training in progress...', color: '#55ff55' },
        { text: 'GPU: NVIDIA RTX 3080 (98% usage)', color: '#ff55ff' },
        { text: 'Estimated time remaining: 2h 34m', color: '#55ffff' },
        { text: '▮', color: '#33ff33' }
      ];

      ctx.font = '11px monospace';
      terminalLines.forEach((line, i) => {
        ctx.fillStyle = line.color;
        ctx.fillText(line.text, 15, 50 + i * 18);
      });

      return canvas;
    }

    function createMiniKeyboard() {
      // Keyboard base (white/light blue)
      const keyboardGeometry = new THREE.BoxGeometry(1.4, 0.08, 0.5);
      const keyboardMaterial = new THREE.MeshStandardMaterial({
        color: 0xe8f4f8,
        roughness: 0.5
      });
      const keyboard = new THREE.Mesh(keyboardGeometry, keyboardMaterial);
      keyboard.position.set(0.05, 1.33, 0.85);
      keyboard.rotation.x = 0.08;
      keyboard.castShadow = true;
      workspace3dGroup.add(keyboard);

      // Keys (simplified grid)
      const keyMaterial = new THREE.MeshStandardMaterial({
        color: 0xb8d4f1,
        roughness: 0.6
      });

      for (let row = 0; row < 4; row++) {
        for (let col = 0; col < 13; col++) {
          const key = new THREE.Mesh(
            new THREE.BoxGeometry(0.09, 0.04, 0.09),
            keyMaterial
          );
          key.position.set(
            -0.6 + col * 0.095,
            1.38,
            0.7 + row * 0.105
          );
          workspace3dGroup.add(key);
        }
      }

      // Mouse
      const mouseBody = new THREE.Mesh(
        new THREE.BoxGeometry(0.2, 0.08, 0.28),
        new THREE.MeshStandardMaterial({
          color: 0xffffff,
          roughness: 0.4,
          metalness: 0.3
        })
      );
      mouseBody.position.set(1.1, 1.33, 0.85);
      mouseBody.castShadow = true;
      workspace3dGroup.add(mouseBody);

      // Mouse top (rounded)
      const mouseTop = new THREE.Mesh(
        new THREE.SphereGeometry(0.1, 16, 16, 0, Math.PI * 2, 0, Math.PI * 0.5),
        new THREE.MeshStandardMaterial({
          color: 0xe8f4f8,
          roughness: 0.3
        })
      );
      mouseTop.position.set(1.1, 1.37, 0.85);
      mouseTop.rotation.x = Math.PI;
      workspace3dGroup.add(mouseTop);

      // Mouse scroll wheel
      const scrollWheel = new THREE.Mesh(
        new THREE.CylinderGeometry(0.02, 0.02, 0.04, 8),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1 })
      );
      scrollWheel.position.set(1.1, 1.39, 0.82);
      scrollWheel.rotation.x = Math.PI / 2;
      workspace3dGroup.add(scrollWheel);
    }

    function createCPUTower() {
      // CPU tower body (black/dark gray case)
      const cpuBody = new THREE.Mesh(
        new THREE.BoxGeometry(0.5, 0.9, 0.45),
        new THREE.MeshStandardMaterial({
          color: 0x2a2a2a,
          roughness: 0.4,
          metalness: 0.6
        })
      );
      cpuBody.position.set(2.0, 0.75, 0.2);
      cpuBody.castShadow = true;
      workspace3dGroup.add(cpuBody);

      // Front panel accent (blue)
      const frontPanel = new THREE.Mesh(
        new THREE.BoxGeometry(0.48, 0.86, 0.02),
        new THREE.MeshStandardMaterial({
          color: 0x1a1a2e,
          roughness: 0.3,
          metalness: 0.7
        })
      );
      frontPanel.position.set(2.0, 0.75, 0.44);
      workspace3dGroup.add(frontPanel);

      // Power button (glowing blue)
      const powerButton = new THREE.Mesh(
        new THREE.CylinderGeometry(0.04, 0.04, 0.02, 16),
        new THREE.MeshStandardMaterial({
          color: 0x4a9eff,
          emissive: 0x4a9eff,
          emissiveIntensity: 0.8
        })
      );
      powerButton.rotation.x = Math.PI / 2;
      powerButton.position.set(2.15, 1.1, 0.45);
      workspace3dGroup.add(powerButton);

      // Power button light
      const powerLight = new THREE.PointLight(0x4a9eff, 0.3, 1);
      powerLight.position.set(2.15, 1.1, 0.5);
      scene.add(powerLight);

      // Ventilation slots
      for (let i = 0; i < 5; i++) {
        const vent = new THREE.Mesh(
          new THREE.BoxGeometry(0.35, 0.02, 0.01),
          new THREE.MeshStandardMaterial({ color: 0x1a1a1a })
        );
        vent.position.set(2.0, 0.5 + i * 0.08, 0.45);
        workspace3dGroup.add(vent);
      }

      // Side panel accent (subtle blue glow - no flickering)
      const sidePanel = new THREE.Mesh(
        new THREE.BoxGeometry(0.02, 0.7, 0.35),
        new THREE.MeshStandardMaterial({
          color: 0x4a9eff,
          emissive: 0x4a9eff,
          emissiveIntensity: 0.15,
          roughness: 0.3
        })
      );
      sidePanel.position.set(2.26, 0.75, 0.2);
      workspace3dGroup.add(sidePanel);

      // Cable from CPU to monitor (simplified)
      const cable = new THREE.Mesh(
        new THREE.CylinderGeometry(0.02, 0.02, 0.6, 8),
        new THREE.MeshStandardMaterial({ color: 0x1a1a1a, roughness: 0.6 })
      );
      cable.position.set(1.5, 1.0, -0.1);
      cable.rotation.z = Math.PI / 4;
      workspace3dGroup.add(cable);
    }

    function createSideTable() {
      // Small side table/nightstand for books and storage
      // Table top
      const tableTop = new THREE.Mesh(
        new THREE.BoxGeometry(1.2, 0.12, 0.9),
        new THREE.MeshStandardMaterial({
          color: 0xd4a574,
          roughness: 0.6
        })
      );
      tableTop.position.set(1.5, 0.8, 2.0);
      tableTop.castShadow = true;
      workspace3dGroup.add(tableTop);

      // Table legs
      const legPositions = [
        [1.0, 2.4], [2.0, 2.4], [1.0, 1.6], [2.0, 1.6]
      ];
      legPositions.forEach(([x, z]) => {
        const leg = new THREE.Mesh(
          new THREE.CylinderGeometry(0.06, 0.06, 0.8, 16),
          new THREE.MeshStandardMaterial({ color: 0xb89060, roughness: 0.7 })
        );
        leg.position.set(x, 0.4, z);
        leg.castShadow = true;
        workspace3dGroup.add(leg);
      });

      // Books on side table (stacked)
      const bookColors = [
        0x8b4513, 0x654321, 0xa0522d, 0xd2691e, 0xff8c00
      ];
      bookColors.forEach((color, i) => {
        const book = new THREE.Mesh(
          new THREE.BoxGeometry(0.35, 0.05, 0.25),
          new THREE.MeshStandardMaterial({ color: color, roughness: 0.8 })
        );
        book.position.set(1.3, 0.89 + i * 0.055, 2.1);
        book.rotation.y = Math.random() * 0.3 - 0.15;
        workspace3dGroup.add(book);
      });

      // More books laid flat
      const flatBookColors = [0x4a8bb7, 0x5a9bc7, 0x7eb3d9];
      flatBookColors.forEach((color, i) => {
        const book = new THREE.Mesh(
          new THREE.BoxGeometry(0.28, 0.04, 0.22),
          new THREE.MeshStandardMaterial({ color: color, roughness: 0.8 })
        );
        book.position.set(1.7, 0.89 + i * 0.045, 1.85);
        book.rotation.y = 0.2;
        workspace3dGroup.add(book);
      });

      // Small plant pot on side table
      const pot = new THREE.Mesh(
        new THREE.CylinderGeometry(0.1, 0.08, 0.15, 16),
        new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.7 })
      );
      pot.position.set(1.8, 0.95, 2.2);
      workspace3dGroup.add(pot);

      // Small plant
      for (let i = 0; i < 3; i++) {
        const leaf = new THREE.Mesh(
          new THREE.SphereGeometry(0.08, 8, 8),
          new THREE.MeshStandardMaterial({ color: 0x4a7c4e, roughness: 0.8 })
        );
        leaf.position.set(
          1.8 + Math.cos(i * 2) * 0.08,
          1.08 + Math.random() * 0.1,
          2.2 + Math.sin(i * 2) * 0.08
        );
        workspace3dGroup.add(leaf);
      }

      // Coffee mug on side table
      const sideMug = new THREE.Mesh(
        new THREE.CylinderGeometry(0.09, 0.07, 0.14, 16),
        new THREE.MeshStandardMaterial({ color: 0xff8585, roughness: 0.5 })
      );
      sideMug.position.set(1.2, 0.95, 1.9);
      workspace3dGroup.add(sideMug);
    }

    function createShelves() {
      // Left side shelves (2 tiers)
      for (let i = 0; i < 2; i++) {
        const shelf = new THREE.Mesh(
          new THREE.BoxGeometry(1.2, 0.08, 0.4),
          new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.7 })
        );
        shelf.position.set(-2, 3 + i * 0.8, -2.4);
        shelf.castShadow = true;
        workspace3dGroup.add(shelf);

        // Shelf support brackets
        [-0.5, 0.5].forEach(xOffset => {
          const bracket = new THREE.Mesh(
            new THREE.BoxGeometry(0.06, 0.3, 0.06),
            new THREE.MeshStandardMaterial({ color: 0xb89060 })
          );
          bracket.position.set(-2 + xOffset, 2.85 + i * 0.8, -2.4);
          workspace3dGroup.add(bracket);
        });

        // Books on shelves
        for (let j = 0; j < 3; j++) {
          const book = new THREE.Mesh(
            new THREE.BoxGeometry(0.15, 0.25, 0.08),
            new THREE.MeshStandardMaterial({
              color: [0x7eb3d9, 0x5a9bc7, 0x4a8bb7][j],
              roughness: 0.8
            })
          );
          book.position.set(-2.3 + j * 0.18, 3.16 + i * 0.8, -2.4);
          book.rotation.y = (Math.random() - 0.5) * 0.2;
          workspace3dGroup.add(book);
        }
      }

      // Top shelf with decorations
      const topShelf = new THREE.Mesh(
        new THREE.BoxGeometry(5, 0.12, 0.5),
        new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.7 })
      );
      topShelf.position.set(0, 4.8, -2.75);
      topShelf.castShadow = true;
      workspace3dGroup.add(topShelf);
    }

    function createWallDecorations() {
      // Window on back wall (right side)
      const windowFrameOuter = new THREE.Mesh(
        new THREE.BoxGeometry(1.1, 1.3, 0.12),
        new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.6 })
      );
      windowFrameOuter.position.set(1.5, 3.2, -2.94);
      workspace3dGroup.add(windowFrameOuter);

      // Window panes (light blue glass)
      const windowGlass = new THREE.Mesh(
        new THREE.BoxGeometry(1, 1.2, 0.05),
        new THREE.MeshStandardMaterial({
          color: 0xb8d4f1,
          transparent: true,
          opacity: 0.5,
          roughness: 0.1
        })
      );
      windowGlass.position.set(1.5, 3.2, -2.88);
      workspace3dGroup.add(windowGlass);

      // Window blinds (rolled up at top)
      const blinds = new THREE.Mesh(
        new THREE.BoxGeometry(1, 0.25, 0.08),
        new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.7 })
      );
      blinds.position.set(1.5, 3.9, -2.9);
      workspace3dGroup.add(blinds);

      // Wall signs/labels (left wall) - wooden style
      const signLabels = ['NETWORK', 'ABOUT', 'CONTACT'];
      signLabels.forEach((label, i) => {
        const sign = new THREE.Mesh(
          new THREE.BoxGeometry(0.7, 0.25, 0.06),
          new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.6 })
        );
        sign.position.set(-2.88, 3.6 - i * 0.45, -1);
        workspace3dGroup.add(sign);
      });

      // Picture frames on back wall
      // Frame 1 - left side
      const frame1 = new THREE.Mesh(
        new THREE.BoxGeometry(0.6, 0.7, 0.05),
        new THREE.MeshStandardMaterial({ color: 0xd4a574, roughness: 0.5 })
      );
      frame1.position.set(-1.2, 3.5, -2.92);
      workspace3dGroup.add(frame1);

      const picture1 = new THREE.Mesh(
        new THREE.BoxGeometry(0.5, 0.6, 0.02),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.7 })
      );
      picture1.position.set(-1.2, 3.5, -2.89);
      workspace3dGroup.add(picture1);

      // Clock on back wall
      const clock = new THREE.Mesh(
        new THREE.CylinderGeometry(0.25, 0.25, 0.08, 32),
        new THREE.MeshStandardMaterial({ color: 0xf5e6d3, roughness: 0.5 })
      );
      clock.rotation.x = Math.PI / 2;
      clock.position.set(0, 3.8, -2.9);
      workspace3dGroup.add(clock);

      // Clock face
      const clockFace = new THREE.Mesh(
        new THREE.CircleGeometry(0.22, 32),
        new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.3 })
      );
      clockFace.position.set(0, 3.8, -2.86);
      workspace3dGroup.add(clockFace);

      // String lights on top
      for (let i = 0; i < 10; i++) {
        const light = new THREE.Mesh(
          new THREE.SphereGeometry(0.08, 16, 16),
          new THREE.MeshStandardMaterial({
            color: [0xffd700, 0xb8d4f1, 0x7eb3d9, 0x98fb98, 0xffb6c1][i % 5],
            emissive: [0xffd700, 0xb8d4f1, 0x7eb3d9, 0x98fb98, 0xffb6c1][i % 5],
            emissiveIntensity: 0.4
          })
        );
        light.position.set(-2.5 + i * 0.55, 4.65, -2.6);
        workspace3dGroup.add(light);

        // Add small point light for each bulb
        const bulbLight = new THREE.PointLight(
          [0xffd700, 0xb8d4f1, 0x7eb3d9, 0x98fb98, 0xffb6c1][i % 5],
          0.2,
          1
        );
        bulbLight.position.set(-2.5 + i * 0.55, 4.65, -2.6);
        scene.add(bulbLight);
      }

      // String connecting the lights
      const stringGeometry = new THREE.CylinderGeometry(0.01, 0.01, 5.5, 8);
      const string = new THREE.Mesh(
        stringGeometry,
        new THREE.MeshStandardMaterial({ color: 0x808080 })
      );
      string.position.set(0, 4.65, -2.6);
      string.rotation.z = Math.PI / 2;
      workspace3dGroup.add(string);

      // Wall shelf decorations
      // Small plant on shelf
      const shelfPlant = new THREE.Mesh(
        new THREE.ConeGeometry(0.12, 0.25, 8),
        new THREE.MeshStandardMaterial({ color: 0x90ee90, roughness: 0.8 })
      );
      shelfPlant.position.set(-1.8, 3.2, -2.35);
      workspace3dGroup.add(shelfPlant);

      // Small pot for shelf plant
      const shelfPot = new THREE.Mesh(
        new THREE.CylinderGeometry(0.08, 0.06, 0.15, 16),
        new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.6 })
      );
      shelfPot.position.set(-1.8, 3.08, -2.35);
      workspace3dGroup.add(shelfPot);

      // Decorative items on top shelf
      const decorColors = [0xb8d4f1, 0x7eb3d9, 0xffd700];
      decorColors.forEach((color, i) => {
        const decor = new THREE.Mesh(
          new THREE.SphereGeometry(0.1, 16, 16),
          new THREE.MeshStandardMaterial({ color: color, roughness: 0.4 })
        );
        decor.position.set(-1 + i * 0.5, 4.92, -2.5);
        workspace3dGroup.add(decor);
      });
    }

    function createFloorItems() {
      // Cute character/mascot (blue cube for now - like Kirby in original)
      const mascot = new THREE.Mesh(
        new THREE.SphereGeometry(0.25, 32, 32),
        new THREE.MeshStandardMaterial({
          color: 0x7eb3d9,
          roughness: 0.6
        })
      );
      mascot.position.set(1.8, 0.7, 2);
      mascot.castShadow = true;
      workspace3dGroup.add(mascot);

      // Eyes for mascot
      [-0.08, 0.08].forEach(xOffset => {
        const eye = new THREE.Mesh(
          new THREE.SphereGeometry(0.04, 16, 16),
          new THREE.MeshStandardMaterial({ color: 0x000000 })
        );
        eye.position.set(1.8 + xOffset, 0.8, 2.2);
        workspace3dGroup.add(eye);
      });

      // Rug/mat on floor
      const rug = new THREE.Mesh(
        new THREE.BoxGeometry(2, 0.02, 1.5),
        new THREE.MeshStandardMaterial({
          color: 0xb8d4f1,
          roughness: 0.9
        })
      );
      rug.position.set(0, 0.38, 1.2);
      workspace3dGroup.add(rug);

      // Small ottoman/stool
      const ottoman = new THREE.Mesh(
        new THREE.CylinderGeometry(0.3, 0.3, 0.25, 32),
        new THREE.MeshStandardMaterial({ color: 0xd4e8f7, roughness: 0.7 })
      );
      ottoman.position.set(-1.5, 0.58, 1.5);
      ottoman.castShadow = true;
      workspace3dGroup.add(ottoman);
    }

    function createPlants() {
      // Plants around the circular base
      const plantPositions = [
        [-4.2, 0, -2],
        [4.2, 0, -2],
        [-3.5, 0, 3.5],
        [3.5, 0, 3.5]
      ];

      plantPositions.forEach(pos => {
        // Pot
        const pot = new THREE.Mesh(
          new THREE.CylinderGeometry(0.25, 0.2, 0.3, 16),
          new THREE.MeshStandardMaterial({ color: 0xb8d4f1, roughness: 0.6 })
        );
        pot.position.set(pos[0], 0.2, pos[2]);
        pot.castShadow = true;
        workspace3dGroup.add(pot);

        // Plant leaves (simple spheres in green)
        for (let i = 0; i < 3; i++) {
          const leaf = new THREE.Mesh(
            new THREE.SphereGeometry(0.15, 16, 16),
            new THREE.MeshStandardMaterial({ color: 0x90ee90, roughness: 0.8 })
          );
          leaf.position.set(
            pos[0] + (Math.random() - 0.5) * 0.2,
            0.4 + i * 0.15,
            pos[2] + (Math.random() - 0.5) * 0.2
          );
          workspace3dGroup.add(leaf);
        }
      });

      // Decorative stones/pebbles around base
      for (let i = 0; i < 12; i++) {
        const angle = (i / 12) * Math.PI * 2;
        const radius = 5.2;
        const stone = new THREE.Mesh(
          new THREE.SphereGeometry(0.12, 16, 16),
          new THREE.MeshStandardMaterial({
            color: 0xe8f4f8,
            roughness: 0.9
          })
        );
        stone.position.set(
          Math.cos(angle) * radius,
          0.15,
          Math.sin(angle) * radius
        );
        stone.scale.y = 0.5;
        workspace3dGroup.add(stone);
      }
    }

    // Keep old functions for compatibility
    function createVSCodeCanvas() { return document.createElement('canvas'); }
    function createLeftMonitor() {}
    function createGitHubCanvas() { return document.createElement('canvas'); }
    function createRightMonitor() {}
    function createKeyboard() {}
    function createMouse() {}
    function createChair() {}
    function createPerson() {}
    function createAccessories() {}
    function createDesk() {}

    function animate() {
      requestAnimationFrame(animate);
      controls.update();
      renderer.render(scene, camera);
    }

    function onWindowResize() {
      const container = document.querySelector('.coding-illustration');
      camera.aspect = container.clientWidth / container.clientHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(container.clientWidth, container.clientHeight);
    }

    // Initialize when DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init3DWorkspace);
    } else {
      init3DWorkspace();
    }

    /*
    // OLD CODE - COMMENTED OUT
    // Typewriter Effect

      // Traffic lights
      ctx.fillStyle = '#ff5f56';
      ctx.beginPath();
      ctx.arc(30, 30, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#ffbd2e';
      ctx.beginPath();
      ctx.arc(65, 30, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#27c93f';
      ctx.beginPath();
      ctx.arc(100, 30, 12, 0, Math.PI * 2);
      ctx.fill();

      // Title
      ctx.fillStyle = '#cccccc';
      ctx.font = 'bold 22px -apple-system, BlinkMacSystemFont, "Segoe UI"';
      ctx.fillText('nlp_model.py - Visual Studio Code', 900, 38);

      // Activity Bar (Left side)
      ctx.fillStyle = '#333333';
      ctx.fillRect(0, 60, 80, 1260);

      // Explorer icon (blue - active)
      ctx.fillStyle = '#007acc';
      ctx.fillRect(20, 90, 40, 40);

      // Other icons
      ctx.fillStyle = '#666666';
      ctx.fillRect(20, 150, 40, 40);
      ctx.fillRect(20, 210, 40, 40);
      ctx.fillRect(20, 270, 40, 40);

      // Sidebar - File Explorer
      ctx.fillStyle = '#252526';
      ctx.fillRect(80, 60, 280, 1260);

      ctx.fillStyle = '#cccccc';
      ctx.font = 'bold 18px Consolas, monospace';
      ctx.fillText('EXPLORER', 100, 100);

      // File tree
      ctx.font = '16px Consolas, monospace';
      ctx.fillStyle = '#8b949e';
      ctx.fillText('▼ AI-NLP-PROJECTS', 100, 135);

      ctx.fillStyle = '#8b949e';
      ctx.fillText('  ▸ data', 110, 165);
      ctx.fillText('  ▸ models', 110, 195);
      ctx.fillText('  ▸ notebooks', 110, 225);
      ctx.fillText('  ▼ src', 110, 255);

      ctx.fillStyle = '#ffffff';
      ctx.fillText('    ▶ nlp_model.py', 120, 285);

      ctx.fillStyle = '#8b949e';
      ctx.fillText('      train.py', 120, 315);
      ctx.fillText('      utils.py', 120, 345);
      ctx.fillText('  .gitignore', 110, 375);
      ctx.fillText('  requirements.txt', 110, 405);
      ctx.fillText('  README.md', 110, 435);

      // Main Editor Area
      ctx.fillStyle = '#1e1e1e';
      ctx.fillRect(360, 60, 1080, 880);

      // Tab
      ctx.fillStyle = '#2d2d2d';
      ctx.fillRect(360, 60, 200, 50);
      ctx.fillStyle = '#ffffff';
      ctx.font = '16px Consolas, monospace';
      ctx.fillText('nlp_model.py', 380, 90);
      ctx.fillStyle = '#666666';
      ctx.fillText('×', 540, 90);

      // Code content
      ctx.font = '15px Consolas, monospace';
      let lineY = 155;
      const lineHeight = 22;

      // Line numbers
      ctx.fillStyle = '#858585';
      for (let i = 1; i <= 30; i++) {
        ctx.fillText(i.toString().padStart(2, ' '), 375, 135 + i * lineHeight);
      }

      // Code
      ctx.fillStyle = '#c586c0';
      ctx.fillText('import', 420, lineY);
      ctx.fillStyle = '#9cdcfe';
      ctx.fillText('torch', 485, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#c586c0';
      ctx.fillText('import', 420, lineY);
      ctx.fillStyle = '#9cdcfe';
      ctx.fillText('transformers', 485, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#c586c0';
      ctx.fillText('from', 420, lineY);
      ctx.fillStyle = '#9cdcfe';
      ctx.fillText('transformers', 475, lineY);
      ctx.fillStyle = '#c586c0';
      ctx.fillText('import', 605, lineY);
      ctx.fillStyle = '#dcdcaa';
      ctx.fillText('BertModel', 670, lineY);
      lineY += lineHeight * 2;

      ctx.fillStyle = '#c586c0';
      ctx.fillText('class', 420, lineY);
      ctx.fillStyle = '#4ec9b0';
      ctx.fillText('NLPModel', 480, lineY);
      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('(nn.Module):', 585, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#c586c0';
      ctx.fillText('    def', 420, lineY);
      ctx.fillStyle = '#dcdcaa';
      ctx.fillText('__init__', 485, lineY);
      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('(self, config):', 575, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('        super().__init__()', 420, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('        self.bert =', 420, lineY);
      ctx.fillStyle = '#dcdcaa';
      ctx.fillText('BertModel', 560, lineY);
      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('.from_pretrained(', 650, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#ce9178';
      ctx.fillText('            "bert-base-uncased"', 420, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('        )', 420, lineY);
      lineY += lineHeight * 2;

      ctx.fillStyle = '#c586c0';
      ctx.fillText('    def', 420, lineY);
      ctx.fillStyle = '#dcdcaa';
      ctx.fillText('forward', 485, lineY);
      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('(self, input_ids):', 560, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('        outputs = self.bert(input_ids)', 420, lineY);
      lineY += lineHeight;

      ctx.fillStyle = '#c586c0';
      ctx.fillText('        return', 420, lineY);
      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('outputs.last_hidden_state', 510, lineY);

      // Copilot Panel (Right side)
      ctx.fillStyle = '#181818';
      ctx.fillRect(1440, 60, 480, 880);

      ctx.fillStyle = '#252526';
      ctx.fillRect(1440, 60, 480, 50);
      ctx.fillStyle = '#cccccc';
      ctx.font = 'bold 16px -apple-system';
      ctx.fillText('GITHUB COPILOT', 1460, 90);

      ctx.font = '14px Consolas, monospace';
      ctx.fillStyle = '#888888';
      ctx.fillText('Suggestions:', 1460, 140);

      // Suggestion boxes
      ctx.fillStyle = '#2d2d2d';
      ctx.fillRect(1460, 160, 420, 100);
      ctx.fillStyle = '#4ec9b0';
      ctx.font = '13px Consolas, monospace';
      ctx.fillText('+ Add attention mechanism', 1475, 190);
      ctx.fillStyle = '#888888';
      ctx.fillText('  for better performance', 1475, 215);

      ctx.fillStyle = '#2d2d2d';
      ctx.fillRect(1460, 280, 420, 100);
      ctx.fillStyle = '#4ec9b0';
      ctx.fillText('+ Implement dropout layers', 1475, 310);
      ctx.fillStyle = '#888888';
      ctx.fillText('  to prevent overfitting', 1475, 335);

      // Terminal at bottom
      ctx.fillStyle = '#1e1e1e';
      ctx.fillRect(360, 940, 1560, 380);

      ctx.fillStyle = '#252526';
      ctx.fillRect(360, 940, 1560, 50);
      ctx.fillStyle = '#cccccc';
      ctx.font = 'bold 16px Consolas, monospace';
      ctx.fillText('TERMINAL', 380, 970);
      ctx.fillStyle = '#007acc';
      ctx.fillText('+', 1880, 970);

      // Terminal content
      ctx.font = '15px Consolas, monospace';
      ctx.fillStyle = '#22c55e';
      ctx.fillText('kavish@macbook', 380, 1020);
      ctx.fillStyle = '#3b82f6';
      ctx.fillText('~/AI-NLP-Projects', 530, 1020);
      ctx.fillStyle = '#d4d4d4';
      ctx.fillText('$', 720, 1020);

      ctx.fillText('python train.py --model bert-base --epochs 10', 380, 1055);

      ctx.fillStyle = '#8b949e';
      ctx.fillText('Epoch 1/10 - Loss: 0.4523 - Accuracy: 0.8234', 380, 1090);
      ctx.fillText('Epoch 2/10 - Loss: 0.3891 - Accuracy: 0.8567', 380, 1115);
      ctx.fillText('Epoch 3/10 - Loss: 0.3245 - Accuracy: 0.8789', 380, 1140);

      ctx.fillStyle = '#3b82f6';
      ctx.fillText('➜  Training in progress...', 380, 1175);

      return canvas;
    }

    function createLeftMonitor() {
      const monitorGroup = new THREE.Group();

      // Monitor frame (bezel)
      const frameGeometry = new THREE.BoxGeometry(5, 3.5, 0.15);
      const frameMaterial = new THREE.MeshStandardMaterial({
        color: 0x000000,
        roughness: 0.3,
        metalness: 0.8
      });
      const frame = new THREE.Mesh(frameGeometry, frameMaterial);
      frame.castShadow = true;
      monitorGroup.add(frame);

      // Screen with VS Code interface
      const vscodeCanvas = createVSCodeCanvas();
      const vscodeTexture = new THREE.CanvasTexture(vscodeCanvas);
      vscodeTexture.minFilter = THREE.LinearFilter;
      vscodeTexture.magFilter = THREE.LinearFilter;

      const screenGeometry = new THREE.BoxGeometry(4.8, 3.3, 0.05);
      const screenMaterial = new THREE.MeshStandardMaterial({
        map: vscodeTexture,
        emissive: 0x1e1e1e,
        emissiveIntensity: 0.3
      });
      const screen = new THREE.Mesh(screenGeometry, screenMaterial);
      screen.position.z = 0.1;
      monitorGroup.add(screen);

      // Screen glow
      const glowGeometry = new THREE.BoxGeometry(4.9, 3.4, 0.01);
      const glowMaterial = new THREE.MeshBasicMaterial({
        color: 0x007acc,
        transparent: true,
        opacity: 0.15
      });
      const glow = new THREE.Mesh(glowGeometry, glowMaterial);
      glow.position.z = 0.15;
      monitorGroup.add(glow);

      // Monitor stand
      const standGeometry = new THREE.CylinderGeometry(0.15, 0.15, 1.5, 16);
      const standMaterial = new THREE.MeshStandardMaterial({
        color: 0x2d3748,
        metalness: 0.7,
        roughness: 0.3
      });
      const stand = new THREE.Mesh(standGeometry, standMaterial);
      stand.position.set(0, -2.5, 0);
      monitorGroup.add(stand);

      // Stand base
      const baseGeometry = new THREE.CylinderGeometry(0.6, 0.6, 0.15, 32);
      const base = new THREE.Mesh(baseGeometry, standMaterial);
      base.position.set(0, -3.3, 0);
      monitorGroup.add(base);

      monitorGroup.position.set(-3.5, 4.5, -2);
      monitorGroup.rotation.y = Math.PI * 0.05;
      workspace3dGroup.add(monitorGroup);
    }

    function createGitHubCanvas() {
      const canvas = document.createElement('canvas');
      canvas.width = 1920;
      canvas.height = 1320;
      const ctx = canvas.getContext('2d');

      // Background
      ctx.fillStyle = '#0d1117';
      ctx.fillRect(0, 0, 1920, 1320);

      // Browser Chrome
      ctx.fillStyle = '#161b22';
      ctx.fillRect(0, 0, 1920, 60);

      // Traffic lights
      ctx.fillStyle = '#ff5f56';
      ctx.beginPath();
      ctx.arc(30, 30, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#ffbd2e';
      ctx.beginPath();
      ctx.arc(65, 30, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#27c93f';
      ctx.beginPath();
      ctx.arc(100, 30, 12, 0, Math.PI * 2);
      ctx.fill();

      // Address bar
      ctx.fillStyle = '#0d1117';
      ctx.fillRect(700, 15, 800, 30);
      ctx.fillStyle = '#58a6ff';
      ctx.font = '18px Consolas, monospace';
      ctx.fillText('github.com/thekavishshah', 720, 38);

      // GitHub Header
      ctx.fillStyle = '#010409';
      ctx.fillRect(0, 60, 1920, 80);

      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 32px -apple-system, BlinkMacSystemFont';
      ctx.fillText('GitHub', 100, 115);

      // Follow button
      ctx.fillStyle = '#238636';
      ctx.fillRect(1650, 85, 200, 50);
      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 20px -apple-system';
      ctx.fillText('Follow', 1720, 118);

      // Profile Section
      ctx.fillStyle = '#0d1117';
      ctx.fillRect(0, 140, 1920, 1180);

      // Profile Picture (circle)
      ctx.fillStyle = '#30363d';
      ctx.beginPath();
      ctx.arc(200, 270, 90, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = '#58a6ff';
      ctx.lineWidth = 4;
      ctx.stroke();

      // Initial
      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 72px -apple-system';
      ctx.fillText('K', 180, 295);

      // Profile Info
      ctx.fillStyle = '#ffffff';
      ctx.font = 'bold 40px -apple-system';
      ctx.fillText('Kavish Shah', 350, 230);

      ctx.fillStyle = '#8b949e';
      ctx.font = '24px -apple-system';
      ctx.fillText('thekavishshah', 350, 270);

      ctx.fillStyle = '#c9d1d9';
      ctx.font = '22px -apple-system';
      ctx.fillText('🎓 CS @ Arizona State University | AI & NLP Enthusiast', 350, 315);

      // Stats Bar
      ctx.fillStyle = '#161b22';
      ctx.fillRect(100, 400, 500, 100);
      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('Repositories', 130, 440);
      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('24', 220, 480);

      ctx.fillStyle = '#161b22';
      ctx.fillRect(650, 400, 500, 100);
      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('Followers', 730, 440);
      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('127', 800, 480);

      ctx.fillStyle = '#161b22';
      ctx.fillRect(1200, 400, 500, 100);
      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('Following', 1285, 440);
      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('89', 1360, 480);

      // Pinned Repositories Title
      ctx.fillStyle = '#c9d1d9';
      ctx.font = 'bold 26px -apple-system';
      ctx.fillText('Pinned Repositories', 100, 580);

      // Repo Card 1 - ClearCue
      ctx.fillStyle = '#161b22';
      ctx.strokeStyle = '#30363d';
      ctx.lineWidth = 2;
      ctx.fillRect(100, 620, 820, 200);
      ctx.strokeRect(100, 620, 820, 200);

      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('ClearCue', 130, 670);

      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('AI-powered study assistant generating flashcards from lecture notes', 130, 710);
      ctx.fillText('using NLP and machine learning', 130, 740);

      // Language indicator
      ctx.fillStyle = '#3572A5';
      ctx.beginPath();
      ctx.arc(130, 790, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#8b949e';
      ctx.font = '18px -apple-system';
      ctx.fillText('Python', 155, 798);

      ctx.fillText('⭐ 18', 750, 798);

      // Repo Card 2 - Portfolio
      ctx.fillStyle = '#161b22';
      ctx.fillRect(1000, 620, 820, 200);
      ctx.strokeRect(1000, 620, 820, 200);

      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('portfolio-website', 1030, 670);

      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('Interactive 3D portfolio showcasing projects and experience', 1030, 710);
      ctx.fillText('Built with React, Three.js, and modern web technologies', 1030, 740);

      // Language indicator
      ctx.fillStyle = '#f1e05a';
      ctx.beginPath();
      ctx.arc(1030, 790, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#8b949e';
      ctx.fillText('JavaScript', 1055, 798);

      ctx.fillText('⭐ 12', 1650, 798);

      // Repo Card 3 - NLP Research
      ctx.fillStyle = '#161b22';
      ctx.fillRect(100, 870, 820, 200);
      ctx.strokeRect(100, 870, 820, 200);

      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('nlp-sentiment-analysis', 130, 920);

      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('Advanced sentiment analysis using BERT transformers', 130, 960);
      ctx.fillText('Fine-tuned on domain-specific datasets', 130, 990);

      ctx.fillStyle = '#3572A5';
      ctx.beginPath();
      ctx.arc(130, 1040, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#8b949e';
      ctx.fillText('Python', 155, 1048);

      ctx.fillText('⭐ 24', 750, 1048);

      // Repo Card 4 - AI Chatbot
      ctx.fillStyle = '#161b22';
      ctx.fillRect(1000, 870, 820, 200);
      ctx.strokeRect(1000, 870, 820, 200);

      ctx.fillStyle = '#58a6ff';
      ctx.font = 'bold 28px -apple-system';
      ctx.fillText('ai-chatbot-engine', 1030, 920);

      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('Conversational AI chatbot with context awareness', 1030, 960);
      ctx.fillText('Powered by GPT architecture and custom training', 1030, 990);

      ctx.fillStyle = '#3572A5';
      ctx.beginPath();
      ctx.arc(1030, 1040, 12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = '#8b949e';
      ctx.fillText('Python', 1055, 1048);

      ctx.fillText('⭐ 31', 1650, 1048);

      // Contribution Graph
      ctx.fillStyle = '#8b949e';
      ctx.font = '20px -apple-system';
      ctx.fillText('342 contributions in the last year', 100, 1150);

      // Contribution squares (simplified grid)
      const colors = ['#0e4429', '#006d32', '#26a641', '#39d353'];
      for (let week = 0; week < 52; week++) {
        for (let day = 0; day < 7; day++) {
          const color = colors[Math.floor(Math.random() * colors.length)];
          ctx.fillStyle = color;
          ctx.fillRect(100 + week * 14, 1180 + day * 14, 12, 12);
        }
      }

      return canvas;
    }

    function createRightMonitor() {
      const monitorGroup = new THREE.Group();

      // Monitor frame
      const frameGeometry = new THREE.BoxGeometry(5, 3.5, 0.15);
      const frameMaterial = new THREE.MeshStandardMaterial({
        color: 0x000000,
        roughness: 0.3,
        metalness: 0.8
      });
      const frame = new THREE.Mesh(frameGeometry, frameMaterial);
      frame.castShadow = true;
      monitorGroup.add(frame);

      // Screen with GitHub profile
      const githubCanvas = createGitHubCanvas();
      const githubTexture = new THREE.CanvasTexture(githubCanvas);
      githubTexture.minFilter = THREE.LinearFilter;
      githubTexture.magFilter = THREE.LinearFilter;

      const screenGeometry = new THREE.BoxGeometry(4.8, 3.3, 0.05);
      const screenMaterial = new THREE.MeshStandardMaterial({
        map: githubTexture,
        emissive: 0x0d1117,
        emissiveIntensity: 0.3
      });
      const screen = new THREE.Mesh(screenGeometry, screenMaterial);
      screen.position.z = 0.1;
      monitorGroup.add(screen);

      // Screen glow
      const glowGeometry = new THREE.BoxGeometry(4.9, 3.4, 0.01);
      const glowMaterial = new THREE.MeshBasicMaterial({
        color: 0x58a6ff,
        transparent: true,
        opacity: 0.15
      });
      const glow = new THREE.Mesh(glowGeometry, glowMaterial);
      glow.position.z = 0.15;
      monitorGroup.add(glow);

      // Monitor stand
      const standGeometry = new THREE.CylinderGeometry(0.15, 0.15, 1.5, 16);
      const standMaterial = new THREE.MeshStandardMaterial({
        color: 0x2d3748,
        metalness: 0.7,
        roughness: 0.3
      });
      const stand = new THREE.Mesh(standGeometry, standMaterial);
      stand.position.set(0, -2.5, 0);
      monitorGroup.add(stand);

      // Stand base
      const baseGeometry = new THREE.CylinderGeometry(0.6, 0.6, 0.15, 32);
      const base = new THREE.Mesh(baseGeometry, standMaterial);
      base.position.set(0, -3.3, 0);
      monitorGroup.add(base);

      monitorGroup.position.set(3.5, 4.5, -2);
      monitorGroup.rotation.y = -Math.PI * 0.05;
      workspace3dGroup.add(monitorGroup);
    }

    function createKeyboard() {
      const keyboardGroup = new THREE.Group();

      // Keyboard base
      const baseGeometry = new THREE.BoxGeometry(4, 0.2, 1.5);
      const baseMaterial = new THREE.MeshStandardMaterial({
        color: 0x1f2937,
        roughness: 0.4,
        metalness: 0.6
      });
      const base = new THREE.Mesh(baseGeometry, baseMaterial);
      base.castShadow = true;
      keyboardGroup.add(base);

      // Keys
      const keyMaterial = new THREE.MeshStandardMaterial({
        color: 0x4b5563,
        roughness: 0.5
      });

      for (let row = 0; row < 4; row++) {
        for (let col = 0; col < 12; col++) {
          const keyGeometry = new THREE.BoxGeometry(0.25, 0.15, 0.25);
          const key = new THREE.Mesh(keyGeometry, keyMaterial);
          key.position.set(
            -1.7 + col * 0.3,
            0.18,
            -0.4 + row * 0.3
          );
          key.castShadow = true;
          keyboardGroup.add(key);
        }
      }

      // RGB underglow
      const glowGeometry = new THREE.BoxGeometry(4, 0.05, 1.5);
      const glowMaterial = new THREE.MeshBasicMaterial({
        color: 0xa78bfa,
        transparent: true,
        opacity: 0.8
      });
      const glow = new THREE.Mesh(glowGeometry, glowMaterial);
      glow.position.y = -0.12;
      keyboardGroup.add(glow);

      keyboardGroup.position.set(0, 2.35, 1.5);
      keyboardGroup.rotation.x = Math.PI * 0.05;
      workspace3dGroup.add(keyboardGroup);
    }

    function createMouse() {
      const mouseGroup = new THREE.Group();

      // Mouse body
      const bodyGeometry = new THREE.BoxGeometry(0.5, 0.3, 0.8);
      const bodyMaterial = new THREE.MeshStandardMaterial({
        color: 0x1f2937,
        roughness: 0.4,
        metalness: 0.6
      });
      const body = new THREE.Mesh(bodyGeometry, bodyMaterial);
      body.castShadow = true;
      mouseGroup.add(body);

      // Mouse top
      const topGeometry = new THREE.BoxGeometry(0.45, 0.05, 0.75);
      const topMaterial = new THREE.MeshStandardMaterial({
        color: 0x374151,
        roughness: 0.3,
        metalness: 0.7
      });
      const top = new THREE.Mesh(topGeometry, topMaterial);
      top.position.y = 0.17;
      mouseGroup.add(top);

      // LED indicator
      const ledGeometry = new THREE.SphereGeometry(0.03, 16, 16);
      const ledMaterial = new THREE.MeshBasicMaterial({
        color: 0x3b82f6,
        emissive: 0x3b82f6
      });
      const led = new THREE.Mesh(ledGeometry, ledMaterial);
      led.position.set(0, 0.2, -0.2);
      mouseGroup.add(led);

      mouseGroup.position.set(4.5, 2.4, 1.5);
      workspace3dGroup.add(mouseGroup);
    }

    function createChair() {
      const chairGroup = new THREE.Group();

      // Chair back
      const backGeometry = new THREE.BoxGeometry(1.8, 2, 0.3);
      const backMaterial = new THREE.MeshStandardMaterial({
        color: 0x374151,
        roughness: 0.6
      });
      const back = new THREE.Mesh(backGeometry, backMaterial);
      back.position.set(0, 2.5, 0);
      back.castShadow = true;
      chairGroup.add(back);

      // Chair seat
      const seatGeometry = new THREE.BoxGeometry(1.8, 0.3, 1.6);
      const seat = new THREE.Mesh(seatGeometry, backMaterial);
      seat.position.set(0, 1.5, 0.5);
      seat.castShadow = true;
      chairGroup.add(seat);

      chairGroup.position.set(0, 0, 4);
      workspace3dGroup.add(chairGroup);
    }

    function createPerson() {
      const personGroup = new THREE.Group();

      // Body (hoodie)
      const bodyGeometry = new THREE.CylinderGeometry(0.6, 0.8, 1.5, 32);
      const bodyMaterial = new THREE.MeshStandardMaterial({
        color: 0x4f46e5,
        roughness: 0.8
      });
      const body = new THREE.Mesh(bodyGeometry, bodyMaterial);
      body.position.y = 2.7;
      body.castShadow = true;
      personGroup.add(body);

      // Head
      const headGeometry = new THREE.SphereGeometry(0.4, 32, 32);
      const headMaterial = new THREE.MeshStandardMaterial({
        color: 0xfbbf24,
        roughness: 0.5
      });
      const head = new THREE.Mesh(headGeometry, headMaterial);
      head.position.y = 3.8;
      head.castShadow = true;
      personGroup.add(head);

      // Hair
      const hairGeometry = new THREE.SphereGeometry(0.42, 32, 32, 0, Math.PI * 2, 0, Math.PI * 0.7);
      const hairMaterial = new THREE.MeshStandardMaterial({
        color: 0x0f172a,
        roughness: 0.9
      });
      const hair = new THREE.Mesh(hairGeometry, hairMaterial);
      hair.position.y = 3.9;
      personGroup.add(hair);

      // Left arm
      const armGeometry = new THREE.CylinderGeometry(0.15, 0.15, 1.5, 16);
      const leftArm = new THREE.Mesh(armGeometry, bodyMaterial);
      leftArm.position.set(-0.65, 2.5, 0.5);
      leftArm.rotation.x = Math.PI * 0.4;
      leftArm.rotation.z = Math.PI * 0.2;
      leftArm.castShadow = true;
      personGroup.add(leftArm);

      // Right arm
      const rightArm = new THREE.Mesh(armGeometry, bodyMaterial);
      rightArm.position.set(0.65, 2.5, 0.5);
      rightArm.rotation.x = Math.PI * 0.4;
      rightArm.rotation.z = -Math.PI * 0.2;
      rightArm.castShadow = true;
      personGroup.add(rightArm);

      // Hands
      const handGeometry = new THREE.SphereGeometry(0.12, 16, 16);
      const handMaterial = new THREE.MeshStandardMaterial({
        color: 0xfbbf24,
        roughness: 0.6
      });

      const leftHand = new THREE.Mesh(handGeometry, handMaterial);
      leftHand.position.set(-1.2, 2.2, 1.5);
      personGroup.add(leftHand);

      const rightHand = new THREE.Mesh(handGeometry, handMaterial);
      rightHand.position.set(1.2, 2.2, 1.5);
      personGroup.add(rightHand);

      personGroup.position.set(0, 0, 3.5);
      workspace3dGroup.add(personGroup);
    }

    function createAccessories() {
      // Coffee cup with "AI Developer" text
      const cupGroup = new THREE.Group();

      const cupGeometry = new THREE.CylinderGeometry(0.3, 0.25, 0.6, 32);
      const cupMaterial = new THREE.MeshStandardMaterial({
        color: 0xD2691E,
        roughness: 0.4,
        metalness: 0.1
      });
      const cup = new THREE.Mesh(cupGeometry, cupMaterial);
      cup.castShadow = true;
      cupGroup.add(cup);

      // Coffee liquid
      const liquidGeometry = new THREE.CylinderGeometry(0.28, 0.24, 0.4, 32);
      const liquidMaterial = new THREE.MeshStandardMaterial({
        color: 0x3e2723,
        roughness: 0.2
      });
      const liquid = new THREE.Mesh(liquidGeometry, liquidMaterial);
      liquid.position.y = 0.15;
      cupGroup.add(liquid);

      cupGroup.position.set(6, 2.6, -1);
      workspace3dGroup.add(cupGroup);

      // Mini plant (succulent)
      const plantGroup = new THREE.Group();

      const potGeometry = new THREE.CylinderGeometry(0.3, 0.25, 0.5, 16);
      const potMaterial = new THREE.MeshStandardMaterial({
        color: 0x7C2D12,
        roughness: 0.7
      });
      const pot = new THREE.Mesh(potGeometry, potMaterial);
      pot.castShadow = true;
      plantGroup.add(pot);

      // Plant leaves (succulent style)
      for (let i = 0; i < 8; i++) {
        const leafGeometry = new THREE.SphereGeometry(0.2, 16, 16);
        const leafMaterial = new THREE.MeshStandardMaterial({
          color: 0x10B981,
          roughness: 0.8
        });
        const leaf = new THREE.Mesh(leafGeometry, leafMaterial);
        const angle = (i / 8) * Math.PI * 2;
        leaf.position.set(
          Math.cos(angle) * 0.25,
          0.5,
          Math.sin(angle) * 0.25
        );
        plantGroup.add(leaf);
      }

      plantGroup.position.set(-6.5, 2.5, -1.5);
      workspace3dGroup.add(plantGroup);

      // AI/ML Book Stack with titles
      const books = [
        { color: 0x2563EB, title: 'Deep Learning' },
        { color: 0xDC2626, title: 'NLP with Transformers' },
        { color: 0x059669, title: 'Python for AI' }
      ];

      books.forEach((book, i) => {
        const bookGeometry = new THREE.BoxGeometry(1.2, 0.18, 0.8);
        const bookMaterial = new THREE.MeshStandardMaterial({
          color: book.color,
          roughness: 0.6,
          metalness: 0.1
        });
        const bookMesh = new THREE.Mesh(bookGeometry, bookMaterial);
        bookMesh.position.set(6.5, 2.4 + i * 0.19, 1.2);
        bookMesh.rotation.y = (Math.random() - 0.5) * 0.15;
        bookMesh.castShadow = true;
        workspace3dGroup.add(bookMesh);
      });

      // Notebook/Journal (opened)
      const notebookBase = new THREE.BoxGeometry(0.8, 0.05, 0.6);
      const notebookMat = new THREE.MeshStandardMaterial({
        color: 0x2d2d2d,
        roughness: 0.8
      });
      const notebook = new THREE.Mesh(notebookBase, notebookMat);
      notebook.position.set(-5, 2.35, 2);
      notebook.rotation.y = -0.4;
      notebook.castShadow = true;
      workspace3dGroup.add(notebook);

      // Pen on notebook
      const penGeometry = new THREE.CylinderGeometry(0.03, 0.03, 0.6, 16);
      const penMaterial = new THREE.MeshStandardMaterial({
        color: 0x1a1a1a,
        roughness: 0.3,
        metalness: 0.7
      });
      const pen = new THREE.Mesh(penGeometry, penMaterial);
      pen.position.set(-4.8, 2.4, 2.1);
      pen.rotation.z = Math.PI / 2;
      pen.rotation.y = -0.4;
      workspace3dGroup.add(pen);

      // Headphone stand with headphones
      const standPoleGeometry = new THREE.CylinderGeometry(0.05, 0.08, 1.2, 16);
      const standMaterial = new THREE.MeshStandardMaterial({
        color: 0x1a1a1a,
        roughness: 0.3,
        metalness: 0.8
      });
      const standPole = new THREE.Mesh(standPoleGeometry, standMaterial);
      standPole.position.set(-7, 2.85, 2);
      workspace3dGroup.add(standPole);

      // Headphone arch
      const headphoneArc = new THREE.TorusGeometry(0.35, 0.05, 16, 32, Math.PI);
      const headphoneMat = new THREE.MeshStandardMaterial({
        color: 0x2d2d2d,
        roughness: 0.4,
        metalness: 0.6
      });
      const headphone = new THREE.Mesh(headphoneArc, headphoneMat);
      headphone.position.set(-7, 3.4, 2);
      headphone.rotation.x = Math.PI / 2;
      workspace3dGroup.add(headphone);

      // Laptop (closed) on side
      const laptopBase = new THREE.BoxGeometry(1.2, 0.08, 0.9);
      const laptopMat = new THREE.MeshStandardMaterial({
        color: 0x2d3748,
        roughness: 0.3,
        metalness: 0.7
      });
      const laptop = new THREE.Mesh(laptopBase, laptopMat);
      laptop.position.set(7, 2.35, 2.5);
      laptop.rotation.y = 0.3;
      laptop.castShadow = true;
      workspace3dGroup.add(laptop);

      // Sticky notes
      const stickyColors = [0xffeb3b, 0xff9800, 0xff5722];
      stickyColors.forEach((color, i) => {
        const stickyGeometry = new THREE.BoxGeometry(0.4, 0.02, 0.4);
        const stickyMaterial = new THREE.MeshStandardMaterial({
          color: color,
          roughness: 0.9
        });
        const sticky = new THREE.Mesh(stickyGeometry, stickyMaterial);
        sticky.position.set(-4.5 + i * 0.45, 2.36, -2.5);
        sticky.rotation.y = (Math.random() - 0.5) * 0.3;
        workspace3dGroup.add(sticky);
      });
    }

    function createFloor() {
      const floorGeometry = new THREE.PlaneGeometry(40, 40);
      const floorMaterial = new THREE.MeshStandardMaterial({
        color: 0x0f0f23,
        roughness: 0.85,
        metalness: 0.15,
        transparent: true,
        opacity: 0.9
      });
      const floor = new THREE.Mesh(floorGeometry, floorMaterial);
      floor.rotation.x = -Math.PI / 2;
      floor.position.y = -1;
      floor.receiveShadow = true;
      scene.add(floor);

      // Add subtle grid lines for depth
      const gridHelper = new THREE.GridHelper(40, 40, 0x6366f1, 0x1a1a35);
      gridHelper.material.opacity = 0.1;
      gridHelper.material.transparent = true;
      gridHelper.position.y = -0.99;
      scene.add(gridHelper);
    }

    function animate() {
      requestAnimationFrame(animate);
      controls.update();
      renderer.render(scene, camera);
    }

    function onWindowResize() {
      const container = document.querySelector('.coding-illustration');
      camera.aspect = container.clientWidth / container.clientHeight;
      camera.updateProjectionMatrix();
      renderer.setSize(container.clientWidth, container.clientHeight);
    }

    // Initialize when DOM is ready
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init3DWorkspace);
    } else {
      init3DWorkspace();
    }
    */

    // Typewriter Effect
    const typedTextSpan = document.querySelector('.typed-text');
    const textArray = ['Software Engineer', 'Peer Mentor', 'AI Enthusiast'];
    const typingDelay = 100, erasingDelay = 50, newTextDelay = 2000;
    let textArrayIndex = 0, charIndex = 0;
    function type() {
      if (charIndex < textArray[textArrayIndex].length) {
        typedTextSpan.textContent += textArray[textArrayIndex].charAt(charIndex++);
        setTimeout(type, typingDelay);
      } else setTimeout(erase, newTextDelay);
    }
    function erase() {
      if (charIndex > 0) {
        typedTextSpan.textContent = textArray[textArrayIndex].substring(0, --charIndex);
        setTimeout(erase, erasingDelay);
      } else {
        textArrayIndex = (textArrayIndex + 1) % textArray.length;
        setTimeout(type, typingDelay + 500);
      }
    }
    document.addEventListener('DOMContentLoaded', () => setTimeout(type, newTextDelay + 250));

    // Scroll Progress Bar
    window.addEventListener('scroll', () => {
      const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
      const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
      const scrolled = (winScroll / height) * 100;
      document.getElementById('progressBar').style.width = scrolled + '%';
    });
  </script>
</body>
</html>


  <!-- Work Experience -->
  <section id="work" class="py-20 bg-white px-6">
    <h2 class="text-3xl font-semibold text-center mb-12">Work Experience</h2>
    <div class="relative max-w-4xl mx-auto">
      <div class="absolute left-1/2 transform -translate-x-1/2 h-full border-l-4 border-gray-300"></div>
      <!-- Intern -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4 text-right pr-6">
          <h3 class="text-xl font-semibold">Software Engineering Intern</h3>
          <p class="text-sm text-gray-500">Dark Alpha Capital LLC (Mar 2025 – Present · Remote)</p>
          <p class="mt-2 text-gray-600">Built a React/Node.js dashboard, boosting efficiency by 30% with search, filtering, and real‑time updates.</p>
        </div>
        <div class="col-span-1 flex justify-center">
          <div class="w-6 h-6 bg-blue-600 rounded-full"></div>
        </div>
        <div class="col-span-4"></div>
      </div>
      <!-- NASA -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4"></div>
        <div class="col-span-1 flex justify-center">
          <div class="w-6 h-6 bg-blue-600 rounded-full"></div>
        </div>
        <div class="col-span-4 pl-6">
          <h3 class="text-xl font-semibold">Researcher</h3>
          <p class="text-sm text-gray-500">NASA L'SPACE Program (May 2024 – Aug 2024 · Remote)</p>
          <p class="mt-2 text-gray-600">Co‑authored a 17‑page proposal on in‑space manufacturing, led planning, and developed a quantum sensor prototype.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Campus Involvement -->
  <section id="campus" class="py-20 bg-gray-50 px-6">
    <h2 class="text-3xl font-semibold text-center mb-12">Campus & Leadership</h2>
    <div class="relative max-w-4xl mx-auto">
      <div class="absolute left-1/2 transform -translate-x-1/2 h-full border-l-4 border-gray-300"></div>
      <!-- Barrett Mentor -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4 text-right pr-6">
          <h3 class="text-xl font-semibold">Barrett Mentor</h3>
          <p class="text-sm text-gray-500">Barrett, The Honors College (Nov 2024 – Present · On-site)</p>
          <p class="mt-2 text-gray-600">Provide one-on-one support, workshops on study skills & planning, and lead community-building events.</p>
        </div>
        <div class="col-span-1 flex justify-center"><div class="w-6 h-6 bg-green-600 rounded-full"></div></div>
        <div class="col-span-4"></div>
      </div>
      <!-- Fulton Ambassador -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4"></div>
        <div class="col-span-1 flex justify-center"><div class="w-6 h-6 bg-green-600 rounded-full"></div></div>
        <div class="col-span-4 pl-6">
          <h3 class="text-xl font-semibold">Fulton Ambassador</h3>
          <p class="text-sm text-gray-500">Ira A. Fulton Schools of Engineering (Oct 2024 – Present · On-site)</p>
          <p class="mt-2 text-gray-600">Represent ASU Engineering to prospective students & partners during outreach events.</p>
        </div>
      </div>
      <!-- Residential Peer Mentor -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4 text-right pr-6">
          <h3 class="text-xl font-semibold">Residential Peer Mentor</h3>
          <p class="text-sm text-gray-500">ASU Engineering Student Hub (Aug 2024 – Present · On-site)</p>
          <p class="mt-2 text-gray-600">Led workshops & events for 2000+ students, organized development series, and supported academic & social success.</p>
        </div>
        <div class="col-span-1 flex justify-center"><div class="w-6 h-6 bg-green-600 rounded-full"></div></div>
        <div class="col-span-4"></div>
      </div>
      <!-- Summer (E2) Camp Counselor -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4"></div>
        <div class="col-span-1 flex justify-center"><div class="w-6 h-6 bg-green-600 rounded-full"></div></div>
        <div class="col-span-4 pl-6">
          <h3 class="text-xl font-semibold"> Summer (E2) Camp Counselor</h3>
          <p class="text-sm text-gray-500">E2 Camp (Jul 2024 – Aug 2024 · On-site)</p>
          <p class="mt-2 text-gray-600">Designed & led educational activities in nature exploration, arts, and interactive learning.</p>
        </div>
      </div>
      <!-- Treasurer -->
      <div class="grid grid-cols-9 items-center mb-12">
        <div class="col-span-4 text-right pr-6">
          <h3 class="text-xl font-semibold">Treasurer</h3>
          <p class="text-sm text-gray-500">SoDA at ASU (Dec 2023 – Jan 2025 · Hybrid)</p>
          <p class="mt-2 text-gray-600">Managed $3.5K budget, secured $2K sponsorships, and improved reporting, reducing errors by 25%.</p>
        </div>
        <div class="col-span-1 flex justify-center"><div class="w-6 h-6 bg-green-600 rounded-full"></div></div>
        <div class="col-span-4"></div>
      </div>
    </div>
  </section>

  <!-- Projects -->
  <section id="projects" class="py-20 px-6">
    <h2 class="text-3xl font-semibold text-center mb-12">Projects</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">SleepSoon Tracker</h3>
        <p class="text-gray-600 mb-4">Tracks sleep patterns and offers personalized tips to optimize rest and productivity.</p>
        <a href="https://github.com/kyleenax/sleep-soon-clone" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">TeachPortal</h3>
        <p class="text-gray-600 mb-4">An online teaching platform connecting students and educators.</p>
        <a href="https://github.com/ChefToan/CSE360-Help-System" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">ClearCue</h3>
        <p class="text-gray-600 mb-4">AI-driven study aid generating cues and flashcards from lecture notes.</p>
        <a href="https://github.com/thekavishshah/ClearCue" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">Portfolio Website</h3>
        <p class="text-gray-600 mb-4">This personal portfolio including projects, experience tree, and contact.</p>
        <a href="https://github.com/thekavishshah/thekavishshah.github.io" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">Bitrix24</h3>
        <p class="text-gray-600 mb-4">Forked deal-sourcing app built with Next.js, Prisma & PostgreSQL.</p>
        <a href="https://github.com/thekavishshah/Bitrix24" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">TicTacToe</h3>
        <p class="text-gray-600 mb-4">Graphical TicTacToe game built with Python & Tkinter.</p>
        <a href="https://github.com/thekavishshah/TicTacToe" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">JobApplier</h3>
        <p class="text-gray-600 mb-4">Automation tool for managing job applications and follow-ups.</p>
        <a href="https://github.com/thekavishshah/JobApplier" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">CWE-798 Project</h3>
        <p class="text-gray-600 mb-4">Honors project demonstrating blocking hard-coded credentials vulnerabilities.</p>
        <a href="https://github.com/thekavishshah/HardCodedCredentials" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">Expense Tracker</h3>
        <p class="text-gray-600 mb-4">Personal app for tracking expenses, built with JavaScript & localStorage.</p>
        <a href="https://github.com/thekavishshah/Expense-Tracker" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">SnakeGame</h3>
        <p class="text-gray-600 mb-4">Python Snake game from high-school hackathon (2nd prize).</p>
        <a href="https://github.com/thekavishshah/SnakeGame" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
      <div class="bg-gray-100 rounded-2xl shadow-lg p-6 hover:shadow-xl transition">
        <h3 class="text-xl font-semibold mb-2">LEGO EV3</h3>
        <p class="text-gray-600 mb-4">EV3 robot projects with LEGO Mindstorms.</p>
        <a href="https://github.com/thekavishshah/LEGO-EV3" target="_blank" class="text-blue-600 hover:underline">View on GitHub</a>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="py-20 px-6 max-w-3xl mx-auto text-center">
    <h2 class="text-3xl font-semibold mb-6">Get In Touch</h2>
    <div class="flex justify-center gap-8 mt-4">
      <!-- Email Icon -->
      <a href="mailto:kshah77@asu.edu" class="text-gray-800 hover:text-gray-600">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-14 h-14" fill="currentColor" viewBox="0 0 24 24">
          <path d="M2.94 5.06C3.32 4.68 3.86 4.5 4.4 4.5h11.2c.54 0 1.08.18 1.46.56.38.38.56.92.56 1.46v6c0 .54-.18 1.08-.56 1.46-.38.38-.92.56-1.46.56H4.4c-.54 0-1.08-.18-1.46-.56C2.56 13.7 2.38 13.16 2.38 12.62v-6c0-.54.18-1.08.56-1.46zM4.4 6.5l5.6 3.65L15.6 6.5H4.4z"/>
        </svg>
      </a>
      <!-- GitHub -->
<a
  href="https://github.com/thekavishshah"
  target="_blank"
  aria-label="GitHub"
  class="text-blue-600 hover:text-blue-800 transition"
>
  <svg
    class="w-10 h-10 fill-current"
    viewBox="0 0 24 24"
    aria-hidden="true"
  >
    <!-- Octocat silhouette only – no outer circle -->
    <path fill-rule="evenodd" clip-rule="evenodd"
      d="M12 .296C5.372.296 0 5.668 0 12.297c0 5.29 3.438 9.771 8.205 11.364.6.112.82-.263.82-.583 0-.288-.01-1.043-.015-2.045-3.338.724-4.043-1.61-4.043-1.61-.547-1.385-1.335-1.754-1.335-1.754-1.09-.744.083-.73.083-.73 1.205.085 1.84 1.237 1.84 1.237 1.07 1.835 2.808 1.304 3.492.998.108-.775.418-1.305.76-1.604-2.667-.302-5.474-1.335-5.474-5.935 0-1.31.47-2.381 1.236-3.221-.123-.303-.537-1.523.118-3.176 0 0 1.008-.323 3.301 1.23.962-.268 1.985-.402 3.005-.407 1.02.005 2.042.139 3.005.407 2.291-1.553 3.299-1.23 3.299-1.23.655 1.653.241 2.873.118 3.176.769.84 1.236 1.91 1.236 3.221 0 4.61-2.81 5.63-5.483 5.923.43.371.817 1.103.817 2.225 0 1.606-.015 2.898-.015 3.289 0 .322.216.698.825.58C20.565 22.066 24 17.586 24 12.297 24 5.668 18.627.296 12 .296Z" />
  </svg>
</a>

      <!-- LinkedIn Icon -->
      <a href="https://www.linkedin.com/in/shah-kavish/" target="_blank" class="text-blue-700 hover:text-blue-500">
        <svg class="w-10 h-10" fill="currentColor" viewBox="0 0 24 24">
          <path d="M4.98 3.5C4.98 4.88 3.87 6 2.49 6S0 4.88 0 3.5 1.11 1 2.49 1s2.49 1.12 2.49 2.5zM.22 8.1h4.54v15.4H.22V8.1zM8.41 8.1h4.35v2.1h.06c.61-1.15 2.11-2.35 4.35-2.35 4.66 0 5.52 3.07 5.52 7.07v8.55h-4.54v-7.58c0-1.8-.03-4.12-2.51-4.12-2.51 0-2.89 1.96-2.89 3.99v7.71H8.41V8.1z"/>
        </svg>
      </a>
    </div>
  </section>

  <footer class="py-6 text-center text-gray-500">
    <p>© 2025 Kavish Shah. All rights reserved.</p>
  </footer>

  <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
  <script>AOS.init({ duration: 800, once: true });</script>
</body>
</html>

